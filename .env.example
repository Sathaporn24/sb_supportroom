# Optional Docker Compose overrides. Copy this file to `.env` at the repository root.
# The defaults below match docker-compose.yml; change a host port if it is already in use.
LOCAL_POSTGRES_DB=supportroom
LOCAL_POSTGRES_USER=supportroom
LOCAL_POSTGRES_PASSWORD=supportroom-local-only
LOCAL_POSTGRES_PORT=55432
LOCAL_BACKEND_PORT=5138
LOCAL_FRONTEND_PORT=3000
