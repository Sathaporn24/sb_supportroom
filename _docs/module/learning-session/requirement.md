# การเรียน 1 ลิงก์ = หลายการเรียนแยกคนละคน + บันทึกความคืบหน้าและรีวิวคำตอบ AI (learning-session) — Requirements

> **เอกสารนี้คือแหล่งความจริงของโมดูล** — `docs/CORE_FEATURE_SPEC.md` (เคาะกันเอง 11 ส.ค. 2026)
> เป็นบันทึกการตัดสินใจเดิมของทีม เก็บไว้เป็นประวัติ ไม่แก้ · เมื่อไม่ตรงกันให้ยึดเอกสารนี้ ·
> เรื่อง table/column เป็นงานของ `system-analyst` ในขั้นถัดไป
>
> **ความสัมพันธ์กับสเปกเดิม (ณ 2026-08-18 หลังทบทวน):** หลักการ §1 ของสเปกเดิม — **"1 ลิงก์ =
> หลายการเรียน แยกคนละคน"** — **ตรงกับเอกสารฉบับนี้** เอกสารนี้ขยายความให้ละเอียดขึ้นและปิดข้อ §5
> ที่สเปกเดิมยังค้างไว้ · ระหว่างวัน 2026-08-18 เคยมีการพลิกเป็น "1 ลิงก์ = 1 การเรียน" ครั้งหนึ่ง
> แต่ **ถูกเพิกถอนในวันเดียวกัน** — ดู `## Change Log` และบล็อกที่ถูกเพิกถอนใน
> `## Declined / Not Pursuing`

## Overview

`sb_supportroom` คือห้องเรียนสาธิตด้วยเสียงสำหรับทีม CS ของ School Bright ใช้สอนคุณครูให้ใช้งาน
ระบบเองโดยไม่ต้องมีคนนั่งประกบ

**เหตุผลตั้งต้นของทั้งโมดูล — คำอธิบาย use case จริงจากเจ้าของโปรเจกต์:**

> **ลิงก์ = สื่อการสอน** ที่ส่งลงกลุ่มไลน์ได้ ใครก็หยิบไปใช้ได้จนกว่าจะหมดอายุ
> **ห้องเรียนเกิดขึ้นตอนที่ผู้ใช้กดเข้าและระบุชื่อตัวเอง** — แต่ละคนเรียนแบบตัวใครตัวมัน 1:1
> ไม่ใช่ห้องประชุมที่ทุกคนอยู่ด้วยกัน

โครงสร้างวันนี้ผูก **1 ลิงก์ = 1 การเรียน** เป็นก้อนเดียวกัน ซึ่งไม่รองรับ use case ข้างบน
การเปลี่ยนแปลงหลักของโมดูลนี้จึงคือ **แยก "ลิงก์" ออกจาก "การเรียน" เป็นสองสิ่ง** —
ลิงก์หนึ่งอันมีการเรียนได้หลายรายการ แต่ละรายการเป็นของคนละคน (และคนเดิมเรียนซ้ำได้หลายรอบ)
สถานะ/ความคืบหน้าเป็นของ *การเรียน* ไม่ใช่ของ *ลิงก์*

บนฐานนั้น โมดูลนี้เพิ่ม:

1. **ผู้เรียนกรอกชื่อตัวเองก่อนเข้าห้อง** (แบบ Zoom) แทนที่ CS จะกรอกให้ล่วงหน้าตอนสร้างลิงก์
2. **บันทึกความคืบหน้า** ทุกครั้งที่เปลี่ยนสไลด์ เพื่อกลับมาเรียนต่อได้ และให้ CS เห็นว่าถึงสไลด์ไหน
3. **แยก "ดูครบทุกสไลด์" ออกจาก "จบแล้ว"** เพราะผู้เรียนกดจบเองกลางคันได้
4. **CS รีวิวคำตอบ AI ว่าถูก/ผิด พร้อมหมายเหตุ** เพื่อรู้ว่าต้องแก้ที่คลังความรู้ ที่ RAG หรือที่ prompt
5. **หน้าสรุปสองมุม** — ผู้เรียนเห็นเฉพาะคำถาม-คำตอบของตัวเอง · CS เห็นเต็มรวมจุดที่ AI ตอบไม่ได้

ผู้ได้ประโยชน์: ทีม CS (แจกลิงก์เดียวลงกลุ่ม แล้วยังรู้ว่าใครเรียนถึงไหน ถามอะไร AI ตอบผิดตรงไหน)
และคุณครู/ผู้เรียนปลายทาง (หยิบลิงก์ในกลุ่มมาเรียนเมื่อไหร่ก็ได้ · ปิดแท็บแล้วกลับมาเรียนต่อจากจุดเดิม)

## Target Users & Roles

| บทบาท | ใครคือใคร | ทำอะไรได้ |
|---|---|---|
| CS (หลังบ้าน) | ทีม Customer Success ของ School Bright | สร้าง/จัดการลิงก์ (หนึ่งลิงก์แจกได้หลายคน) · ดูรายการ **การเรียนทุกรายการที่เกิดใต้ลิงก์นั้น** · ดูสรุปเต็มรายการเรียน · รีวิวคำตอบ AI ว่าถูก/ผิด + เขียนหมายเหตุ |
| ผู้เรียน | คุณครู/พนักงานปลายทางที่หยิบลิงก์จากกลุ่มไลน์ | เปิดลิงก์ · กรอกชื่อตัวเอง · เรียนแบบตัวใครตัวมัน · ถามคำถาม · กดจบเอง · กด "เรียนอีกครั้ง" · ดูสรุปคำถาม-คำตอบ **ของการเรียนตัวเองเท่านั้น** |

ข้อจำกัดที่รับรู้อยู่แล้ว: ระบบยังไม่มี auth (TD-002 — `/admin/*` เปิดสาธารณะเมื่อ deploy) โมดูลนี้
**ไม่ได้แก้เรื่อง auth** แต่ทำให้ขอบเขต "ข้อมูลใครเห็นได้บ้าง" ชัดขึ้น — ดู Constraints

## Core Features

### F1 — "ลิงก์" กับ "การเรียน" เป็นคนละสิ่ง (การเปลี่ยนแปลงหลัก)

```
เดิม (โค้ดวันนี้)   1 ลิงก์ = 1 การเรียน          ผูกติดกันเป็นก้อนเดียว
ใหม่                1 ลิงก์ = หลายการเรียน แยกคนละคน
```

**ลิงก์** คือสื่อการสอนที่ส่งลงกลุ่มไลน์ได้ ใครก็หยิบไปใช้ได้จนกว่าจะหมดอายุ
**การเรียน** เกิดขึ้นตอนที่ผู้ใช้กดเข้าและระบุชื่อตัวเอง — หนึ่งรายการต่อหนึ่งคนหนึ่งรอบ

CS กรอกตอนสร้างลิงก์: **บทเรียน** · **หน่วยงาน** (ไม่บังคับ) · **วันหมดอายุ** ·
**จำนวนคนสูงสุดต่อลิงก์** (ไม่บังคับ — ดู F8)
CS **ไม่กรอกชื่อผู้เรียนอีกต่อไป** — ผู้เรียนกรอกเองตอนเข้าห้อง (F2)

- "หน่วยงาน" เป็นคำกลางที่ครอบทั้งโรงเรียน สาขาธนาคาร และแผนกในบริษัท
  placeholder ที่เคาะไว้: `เช่น โรงเรียนวัดโพธิ์ / สาขาสีลม / ฝ่ายบุคคล`
- label "หน่วยงาน" มีแค่ CS เห็น ผู้เรียนเห็นเฉพาะ *ค่า* ที่ CS พิมพ์ จึงยังไม่ต้องตั้งค่าต่อบริษัท
- **ลิงก์หมดอายุระหว่างมีคนเรียนค้างอยู่ → ให้การเรียนที่ค้างอยู่ทุกรายการดำเนินต่อจนจบ ไม่ตัดกลางคัน**
  แต่หลังหมดอายุแล้วจะ *เริ่ม* การเรียนใหม่บนลิงก์นั้นไม่ได้ (เคาะ 2026-08-18)
- **ข้อมูลที่เป็นของลิงก์**: บทเรียนที่ผูก · หน่วยงาน · วันหมดอายุ · จำนวนคนสูงสุด ·
  บริษัทเจ้าของ (ตามแบบแผน multi-company เดิม) · audit ผู้สร้าง/เวลาสร้าง
- **ข้อมูลที่ต้องย้ายออกจากลิงก์ไปเป็นของการเรียน**: ชื่อผู้เรียน · สถานะ · เวลาเริ่ม/เวลาจบ ·
  ดูครบทุกสไลด์ · สไลด์ล่าสุด — วันนี้ทั้งหมดนี้อยู่บน `TrainingSession` ซึ่งจะกลายเป็น "ลิงก์"
  (สเปกเดิม §3.1) เก็บไว้ที่เดิมไม่ได้ เพราะลิงก์เดียวมีได้หลายคน หลายสถานะ หลายความคืบหน้าพร้อมกัน

### F1b — ตาราง "การเรียนของแต่ละคน" (ตารางใหม่)

ต้องมีที่เก็บใหม่หนึ่งชุดต่อ "การเรียนหนึ่งรอบของหนึ่งคน" (สเปกเดิม §3.2) —
**นี่คือของใหม่ที่ยังไม่มีในโค้ดวันนี้เลย** และเป็นหัวใจของโมดูล

เหตุผลที่ต้องเป็นตารางใหม่ ไม่ใช่เพิ่มคอลัมน์บนของเดิม: จำนวนการเรียนต่อลิงก์ไม่จำกัดและไม่รู้ล่วงหน้า
คนหนึ่งคนยังเรียนซ้ำได้หลายรอบ (F5) — โครงสร้างแถวเดียวต่อลิงก์รองรับไม่ได้ทั้งสองข้อ

ข้อมูลที่กติกาต้องเก็บได้ต่อการเรียนหนึ่งรายการ:

| เก็บอะไร | ทำไม |
|---|---|
| ลิงก์ที่ใช้เข้ามา | ผูกกลับไปหาบทเรียน/หน่วยงาน/วันหมดอายุ |
| บริษัทเจ้าของ | แบบแผน multi-company เดิม ต้องมี query filter |
| กุญแจผู้เรียน (`LearnerKey`) | แยกคน + กลับมาเรียนต่อ — ดู F3 |
| ชื่อผู้เรียน | ผู้เรียนกรอกเอง (F2) |
| สถานะ (กำลังเรียน / จบแล้ว) | CS ดูภาพรวมได้ |
| เวลาเริ่ม · เวลาจบ | ลำดับเวลา + ใช้แยกรายการที่ชื่อซ้ำกัน |
| เวลาที่มีความเคลื่อนไหวล่าสุด | F6 ใช้คำนวณ "หยุดกลางคัน" |
| สไลด์ล่าสุด (id) + ลำดับที่ของสไลด์ | กลับมาเรียนต่อ (F3) + แสดง "7/20" (F4) |
| ดูครบทุกสไลด์แล้วหรือยัง | แยกจาก "จบแล้ว" ตาม F5 |
| audit ผู้สร้าง/เวลาสร้าง | ตามแบบแผนเดิมของโปรเจกต์ |

### F2 — ผู้เรียนกรอกชื่อตัวเองก่อนเข้าห้อง

เปิดลิงก์ → หน้ากรอกชื่อ → กดเข้า → เริ่มเรียน

- **กรอกชื่ออย่างเดียว ไม่เอาเบอร์โทร อีเมล หรือตำแหน่ง** — ตั้งใจเลี่ยงข้อมูลส่วนบุคคล
  (เคาะ 2026-08-18) ผลที่ตามมา: เฟสนี้ **ไม่ต้องติด 🔒 Security gate ด้วยเหตุผลเรื่อง PII**
- **นี่คือการย้ายจุดกรอก ไม่ใช่การเพิ่มฟิลด์ใหม่ล้วนๆ** — วันนี้ชื่อผู้เรียนถูกกรอกโดย *คนสร้างลิงก์*
  (ดูคอมเมนต์บน `RecipientName` ใน `TrainingSession.cs`: "Free text typed by whoever created
  the session") เฟสนี้เปลี่ยนให้ **ผู้เรียนเป็นคนกรอกเอง** ตอนเข้าห้อง และค่าไปอยู่ที่
  *การเรียน* ไม่ใช่ที่ *ลิงก์* (F1) — ลิงก์เดียวมีได้หลายชื่อ
- ชื่อยังคงเป็น **ป้ายกำกับ ไม่ใช่การยืนยันตัวตน** ตามเดิม — ไม่มีการตรวจสอบว่าเป็นคนนั้นจริง
- **กดเข้า = สร้างการเรียนรายการใหม่** หนึ่งแถวต่อหนึ่งคนหนึ่งรอบ

### F3 — กุญแจผู้เรียนทำสองหน้าที่

ระบบเก็บ **กุญแจผู้เรียน (`LearnerKey`)** ไว้ที่ browser ของผู้เรียนตอนเข้าห้อง (สเปกเดิม §2.4)
กุญแจนี้ทำ **สองหน้าที่พร้อมกัน** — ขาดข้อใดข้อหนึ่งไม่ได้:

1. **กลับมาเรียนต่อ** — เน็ตหลุด ปิดแท็บ ปิดคอม แล้วเปิดลิงก์เดิมบนเครื่อง/เบราว์เซอร์เดิม
   → กลับไปยังการเรียนรายการเดิมได้ **หลังยืนยันตัวตนหนึ่งครั้ง** (ดูกติกาข้างล่าง)
   ไม่ต้องกรอกชื่อใหม่ถ้ายืนยันว่าเป็นคนเดิม
2. **แยกว่าใครเป็นใครบนลิงก์เดียวกัน** — **คนที่สองที่เปิดลิงก์เดิมต้องไม่เห็นของคนแรก**
   ทั้งความคืบหน้าและคำถาม-คำตอบ · ลิงก์เป็นของกลางที่แจกในกลุ่มไลน์ จึงใช้ตัวลิงก์เอง
   เป็นตัวตัดสินว่าใครเป็นใครไม่ได้

**สองกรณีนี้เป็นคนละเรื่องกัน อย่าอ่านปนกัน** — ต่างกันที่ "มีกุญแจอยู่หรือไม่":

#### กรณี ก — ไม่มีกุญแจเลย (ล้าง browser storage / เปลี่ยนเครื่อง / เปิดคนละเบราว์เซอร์)

ระบบไม่มีอะไรให้ผูกกลับ → **กลายเป็น "คนใหม่"** (เคาะ 2026-08-18 — คำตอบ Q6 เดิม ยังคงเดิม)

- ผู้เรียนจะเริ่มการเรียนรอบใหม่จากศูนย์ กรอกชื่อใหม่ ไม่ได้ต่อของเดิม — **ยอมรับได้
  ไม่ต้องทำอะไรเพิ่มในเฟสนี้**
- ผลข้างเคียงที่ยอมรับแล้วเช่นกัน: ฝั่ง CS จะเห็นเป็นสองรายการที่แยกไม่ออกว่าเป็นคนเดียวกัน
  เหมือนกรณี **คนละคนแต่ชื่อซ้ำกัน** บนลิงก์เดียวกัน
- เหตุผล: กรณีนี้เกิดไม่บ่อย · CS อ่านจากเวลา/ความคืบหน้าแทนได้ · และตรงกับหลัก
  "อย่าสร้าง infrastructure ล่วงหน้า" ของโปรเจกต์

#### กรณี ข — มีกุญแจ และเจอการเรียนที่ยังไม่จบผูกอยู่กับกุญแจนี้บนลิงก์นี้

**ต้องถามยืนยันก่อนเสมอ ห้ามพาเข้าไปเรียนต่อเงียบๆ** (เคาะ 2026-08-18)

- แสดงคำถามทำนอง **"คุณคือ *(ชื่อที่บันทึกไว้)* ใช่ไหม"** พร้อมทางเลือกสองทาง:
  - **ใช่** → เข้าไปเรียนต่อจากสไลด์เดิมของการเรียนรายการนั้น
  - **เริ่มเรียนใหม่ในชื่ออื่น** → ไปหน้ากรอกชื่อ (F2) แล้วสร้างการเรียน**รายการใหม่**
    รายการเดิมไม่ถูกลบ ไม่ถูกเขียนทับ และคำถามใหม่ต้องไม่ไปกองรวมกับของรายการเดิม
- **เหตุผลที่ต้องถาม อย่า "ปรับปรุง" ให้กลับไป resume เงียบๆ:** กุญแจอยู่ที่ *เบราว์เซอร์*
  ไม่ใช่ที่ *คน* — บนเครื่องที่ใช้ร่วมกัน (คอมกลางในโรงเรียน ห้องสมุด) คนละคนใช้กุญแจตัวเดียวกัน
  ถ้า resume เงียบๆ คนที่สองจะถูกพาเข้าไปเรียนต่อของคนแรก **เห็นความคืบหน้าและคำถามของคนแรก**
  และคำถามใหม่จะไปกองรวมกับของคนแรกในรายงานที่ CS อ่าน
- **ต้นทุนที่ยอมรับแล้ว:** ผู้เรียนคนเดิมต้องกดยืนยันเพิ่มหนึ่งครั้งทุกครั้งที่กลับเข้ามาหลังเน็ตหลุด/ปิดแท็บ
- เลือกวิธีนี้เพราะแก้ปัญหาเครื่องใช้ร่วมกันได้โดย **ไม่ต้องเพิ่ม login/OTP หรือระบบระบุตัวตนใดๆ** —
  **อย่าเสนอ login/OTP/รหัสยืนยันในเฟสนี้** (ยังคงเป็นข้อห้ามเดิม)
- ยังเป็นไปตาม F2: ชื่อคือ **ป้ายกำกับ ไม่ใช่การยืนยันตัวตน** — การกดยืนยันนี้คือการให้ผู้ใช้
  *เลือกเอง* ว่าจะเรียนต่อหรือเริ่มใหม่ ไม่ใช่การพิสูจน์ตัวตน

### F4 — บันทึกความคืบหน้าล่าสุด

ทุกครั้งที่เปลี่ยนสไลด์ ระบบอัปเดต **ความคืบหน้าล่าสุดของการเรียนรายการนั้น** (อัปเดตแถวเดิม
ไม่สร้างแถวใหม่ต่อสไลด์) — ความคืบหน้าเป็นของ *การเรียน* ไม่ใช่ของ *ลิงก์*

- ต้องเก็บได้พอที่จะ (ก) พาผู้เรียนกลับไปยังสไลด์เดิม และ (ข) แสดงเป็นตัวเลข "7/20" ให้ CS เห็น
  โดยไม่ต้องไป resolve deck ใหม่ (วันนี้เก็บแค่ id ของสไลด์ ยังไม่มีลำดับที่)
- ต้องเก็บ **เวลาที่มีความเคลื่อนไหวล่าสุด** เพราะ F6 ใช้คำนวณ (ยังไม่มีในโครงสร้างวันนี้)

### F5 — "เรียนจบ" นับสองค่าแยกกัน + หน้าสรุป

**"เรียนจบ" เกิดได้สองทาง: ดูครบทุกสไลด์ หรือผู้เรียนกดปุ่มจบเอง** (เคาะ 2026-08-18)
จึงต้องเก็บ **สองค่าแยกจากกัน** ห้ามยุบเป็นค่าเดียว:

| ค่า | ความหมาย |
|---|---|
| ดูครบทุกสไลด์แล้วหรือยัง | ผู้เรียนไปถึงสไลด์สุดท้ายจริงไหม |
| จบแล้วหรือยัง (+ เวลาที่จบ) | การเรียนนี้ปิดแล้วไหม ไม่ว่าจะครบสไลด์หรือไม่ |

เหตุผล: CS ต้องแยกออกว่าใคร "กดจบกลางคัน" (จบแล้ว แต่ไม่ครบสไลด์) ซึ่งเป็นสัญญาณคนละแบบกับคนที่เรียนครบ

หน้าสรุปมองได้สองมุม:

| ใคร | เห็นอะไร |
|---|---|
| ผู้เรียน | "เรียนจบแล้ว" + **คำถาม-คำตอบของการเรียนตัวเอง** + ปุ่ม **"เรียนอีกครั้ง"** |
| CS (หลังบ้าน) | สรุปเต็มของการเรียนรายการนั้น: คำถามทั้งหมด · จุดที่ AI ตอบไม่ได้ · ช่องรีวิว |

กติกาสำคัญ: ผู้เรียน **ไม่เห็น** รายการ "จุดที่ AI ตอบไม่ได้ รอ CS ตรวจสอบ" — เป็นข้อมูลภายในของ CS
"จุดที่ตอบไม่ได้" ไม่ต้องเก็บซ้ำ — คำนวณจากคำถามที่ AI รายงานว่าไม่พบข้อมูล (`not_found`) ได้อยู่แล้ว

**ปุ่ม "เรียนอีกครั้ง" = สร้างการเรียนรอบใหม่** (สเปกเดิม §2.5) — คนหนึ่งคนมีได้หลายรอบบนลิงก์เดียวกัน
รอบเก่าไม่ถูกลบหรือเขียนทับ CS ยังเห็นทุกรอบ · **"จบแล้ว" ไม่ได้แปลว่าจบถาวร**

### F6 — "หยุดกลางคัน" คิดตอนแสดงผล ไม่เก็บเป็นสถานะ

ไม่มีสถานะ "หยุดกลางคัน" ในฐานข้อมูล แต่คำนวณตอนแสดงผล:

```
(ตอนนี้ - เวลาที่มีความเคลื่อนไหวล่าสุด) > INACTIVE_THRESHOLD_MINUTES  →  แสดงว่า "หยุดกลางคัน"
```

- ค่าเริ่มต้น **30 นาที** ปรับผ่าน env ตามแบบแผนเดิมของโปรเจกต์ (`ServerDefaults.cs`)
- เหตุผลที่ไม่ให้ browser ส่งสัญญาณตอนปิดแท็บ: สัญญาณนั้นพลาดบ่อย (ปิดคอม เน็ตหลุด แบตหมด =
  ไม่มีสัญญาณ) และเปลี่ยนใจทีหลังไม่มีต้นทุน เพราะไม่ได้เก็บลง DB

### F7 — CS รีวิวคำตอบ AI (ถูก/ผิด + หมายเหตุ)

CS ทำเครื่องหมาย **ถูก/ผิด** ต่อคำถามหนึ่งข้อ และเขียน **หมายเหตุแบบข้อความอิสระ** (ไม่บังคับ)

เหตุผลที่ต้องมีช่องหมายเหตุ — "ตอบผิด" มีได้อย่างน้อย 3 สาเหตุที่แก้คนละทางกัน:

1. คลังความรู้ไม่มีข้อมูลนี้จริง → เพิ่มเอกสาร
2. มีข้อมูลอยู่ แต่ค้นหาไม่เจอ → ปรับ RAG (threshold / chunk)
3. AI เดาเอง ตอบจากความรู้ทั่วไป → แก้ prompt

ถ้าเก็บแค่ถูก/ผิดจะแยกไม่ออก แล้วมักจบลงที่ "เพิ่มเอกสาร" ทั้งที่ปัญหาจริงคือข้อ 3
เลือก **ช่องข้อความอิสระแทน enum โดยตั้งใจ** เพราะยังไม่รู้ว่าจะเจอสาเหตุแบบไหนอีกบ้าง —
อย่าเปลี่ยนเป็น dropdown/enum ในเฟสนี้

ข้อมูลที่กติกาต้องเก็บได้ต่อคำถาม: ผลรีวิว (ถูก/ผิด/ยังไม่รีวิว) · หมายเหตุ (ว่างได้) · เวลาที่รีวิว (ว่างได้)

**คำถามและบทสนทนาต้องผูกกับ "การเรียน" แทน "ลิงก์"** (สเปกเดิม §3.3, §3.4) — วันนี้ผูกกับลิงก์
ซึ่งใช้ไม่ได้เมื่อลิงก์เดียวมีหลายคน คำถามของคนที่สองจะไปปนกับของคนแรก และหน้าสรุปฝั่งผู้เรียน
(F5) จะรั่วข้ามคน

### F8 — ช่อง "จำนวนคนสูงสุดต่อลิงก์" (กรอกได้ แต่ยังไม่บังคับใช้)

**โชว์ช่องให้ CS กรอกได้ตอนสร้างลิงก์ แต่ระบบยังไม่บังคับใช้ในเฟสนี้** (เคาะ 2026-08-18)

- ว่างไว้ = ไม่จำกัด
- **ต้องมีข้อความกำกับใน UI ว่าค่านี้ยังไม่มีผล** เพื่อไม่ให้ CS เข้าใจผิดว่าระบบกันคนเกินให้แล้ว
- เหตุผลที่เตรียมที่เก็บไว้ก่อน: เพิ่ม *คอลัมน์* ทีหลังเจ็บกว่าเตรียมไว้ตั้งแต่แรก และค่าที่ CS
  กรอกไว้จะใช้ได้ทันทีเมื่อวันหนึ่งเปิดการบังคับใช้
- **การ *บังคับใช้* ยังคงถูกตัดออกตามมติ 2026-08-11** — ดู `## Declined / Not Pursuing`

## Scope

> **นี่คืองานเปลี่ยนโครงสร้างข้อมูล ไม่ใช่งานเติมฟิลด์** — แกนกลางคือการแยก "ลิงก์" ออกจาก
> "การเรียน" ซึ่งลากไปถึงตารางใหม่, การย้าย 6 ฟิลด์ออกจาก `TrainingSession`, การย้ายความสัมพันธ์
> ของ `SessionQuestion`/`ChatMessage` และ migration ของข้อมูลเดิม ประเมินขนาดงานตามนี้

### สถานะโค้ดวันนี้ (ตรวจแล้ว 2026-08-18) — ทั้งหมดนี้คือของที่ต้อง *ย้าย/ปรับ* ไม่ใช่ "มีแล้วไม่ต้องทำ"

`backend/src/SupportRoom.Domain/Entities/TrainingSession.cs` มี: `Token` · `LessonId` ·
`LessonSlug` · `RecipientName` · `RecipientOrgName` · `Status` · `ExpiresAt` · `StartedAt` ·
`EndedAt` · `CompletedAllSlides` · `LastSlideObjectId` · `CompanyId` + audit fields

- อยู่ที่เดิมได้ (เป็นของ "ลิงก์"): `Token` · `LessonId` · `LessonSlug` · `RecipientOrgName` ·
  `ExpiresAt` · `CompanyId` + audit
- **ต้องย้ายไปตารางใหม่**: `RecipientName` · `Status` · `StartedAt` · `EndedAt` ·
  `CompletedAllSlides` · `LastSlideObjectId`
- `SessionQuestion` / `ChatMessage` วันนี้ผูกกับ `TrainingSession` — **ต้องย้ายไปผูกกับ "การเรียน"**
- **ยังไม่มีในโค้ดเลย**: `LearnerKey` · `LastActivityAt` · `LastSlideIndex` · `MaxAttendees` ·
  env `INACTIVE_THRESHOLD_MINUTES` · `ReviewResult`/`ReviewNote`/`ReviewedAt` · ตารางการเรียน ·
  หน้ากรอกชื่อ · ปุ่มกดจบเอง · ปุ่มเรียนอีกครั้ง · หน้ารีวิวของ CS

### MVP (must-have)

- **F1** แยกลิงก์ออกจากการเรียน: `TrainingSession` กลายเป็น "ลิงก์" · ย้าย 6 ฟิลด์ออก ·
  ฟอร์มสร้างลิงก์ของ CS เอาช่อง "ชื่อผู้เรียน" ออก คงหน่วยงาน/วันหมดอายุ เพิ่มช่องจำนวนคนสูงสุด ·
  กติกาหมดอายุ = ห้ามเริ่มการเรียนใหม่ แต่ไม่ตัดรายการที่ค้างอยู่
- **F1b** ตารางใหม่ "การเรียนของแต่ละคน" ครบทุกค่าตามตารางใน F1b + migration ย้ายข้อมูลเดิม
- **F2** หน้าให้ผู้เรียนกรอกชื่อก่อนเข้าห้อง (ชื่ออย่างเดียว) · กดเข้า = สร้างการเรียนรายการใหม่
- **F3** `LearnerKey` ใน browser ทำสองหน้าที่ (เรียนต่อ + แยกคนบนลิงก์เดียวกัน) ·
  คนที่สองต้องไม่เห็นของคนแรก · **หน้ายืนยัน "คุณคือ (ชื่อ) ใช่ไหม" ก่อนเรียนต่อทุกครั้ง
  พร้อมทางเลือก "เริ่มเรียนใหม่ในชื่ออื่น"** — ห้าม resume เงียบๆ (กรณี ข ใน F3)
- **F4** บันทึกความคืบหน้า + **เวลาที่มีความเคลื่อนไหวล่าสุด** + **ลำดับที่ของสไลด์** (แสดง "7/20")
- **F5** ปุ่มให้ผู้เรียนกดจบเอง + เก็บ "จบแล้ว" แยกจาก "ครบทุกสไลด์" + หน้าสรุปฝั่งผู้เรียน +
  **ปุ่ม "เรียนอีกครั้ง"** (สร้างรอบใหม่)
- **F6** แสดง "หยุดกลางคัน" แบบคำนวณ พร้อม env `INACTIVE_THRESHOLD_MINUTES` (ค่าเริ่มต้น 30)
- **F7** ย้าย `SessionQuestion`/`ChatMessage` ไปผูกกับการเรียน + เพิ่มผลรีวิว/หมายเหตุ/เวลารีวิว
  บน `SessionQuestion` + หน้ารีวิวของ CS
- **F8** ช่อง "จำนวนคนสูงสุดต่อลิงก์" ที่กรอกได้แต่ยังไม่บังคับใช้ + ข้อความกำกับใน UI

### Later (nice-to-have)

- Event log ต่อสไลด์ / dashboard ว่าคนหลุดที่สไลด์ไหน
- สถานะ "จัดการแล้ว" ในรายการรีวิว
- ตั้งค่า label "หน่วยงาน" ต่อบริษัท (รอจนมี auth และตาราง `Company` จริง)
- ห้องกลุ่ม — เป็นลิงก์คนละชนิด แยกจากของเดิม ไม่ยัดเข้ามาในโมดูลนี้

### Pre-existing assets reconciled

`docs/CORE_FEATURE_SPEC.md` §3 คือ schema ที่เจ้าของโปรเจกต์ร่างไว้ล่วงหน้าแล้ว ไล่ทีละชิ้น
**ทบทวนตารางนี้ใหม่ทั้งหมดเมื่อ 2026-08-18 หลังเพิกถอนการพลิกเป็น 1:1** — กลับมาตรงกับสเปกเดิม
(ยืนยันจากโค้ดจริง — `backend/src/SupportRoom.Domain/Entities/` ยังมี 6 entity เดิม, ไม่มี `MaxAttendees`)

| ชิ้นงานที่มีอยู่แล้ว | สถานะ |
|---|---|
| `TrainingSession` → กลายเป็น "ลิงก์เชิญ" (สเปกเดิม §3.1) | **MVP** |
| `TrainingSession.RecipientOrgName` (ช่องหน่วยงาน) | **MVP — มีในโค้ดแล้ว อยู่ที่เดิมได้** |
| `TrainingSession` → ย้าย `RecipientName`/`Status`/`StartedAt`/`EndedAt`/`CompletedAllSlides`/`LastSlideObjectId` ออกไปตารางใหม่ (§3.1) | **MVP** |
| `TrainingSession.RecipientName` → เปลี่ยน *ผู้กรอก* จาก CS เป็นผู้เรียน และย้ายไปอยู่ที่การเรียน | **MVP** |
| `TrainingSession` → `MaxAttendees` (ช่องกรอก) | **MVP — เตรียมที่เก็บ + ช่องกรอก แต่ยังไม่บังคับใช้** (F8) |
| การ *บังคับใช้* จำกัดจำนวนคนต่อลิงก์ (§6) | **Declined 2026-08-11 — ยังคงเดิม** |
| `TrainingSession` → จะ rename ให้สื่อว่าเป็น "ลิงก์" หรือไม่ | **ยังเปิดอยู่ — `system-analyst` ตัดสิน** (Q2) |
| ตารางใหม่ "การเรียนของแต่ละคน" (§3.2) | **MVP — เป็นหัวใจของโมดูล** (F1b) |
| ชื่อของตารางใหม่ | **ยังเปิดอยู่ — `system-analyst` ตัดสิน** (Q3) |
| `LearnerKey` (key ใน browser) | **MVP — ทำสองหน้าที่ตาม §2.4** (F3) |
| `SessionQuestion` / `ChatMessage` → ย้ายไปผูกกับ "การเรียน" แทน "ลิงก์" (§3.3, §3.4) | **MVP** |
| `SessionQuestion` → เพิ่มผลรีวิว/หมายเหตุ/เวลารีวิว | **MVP** |
| การเรียน → เพิ่มเวลาเคลื่อนไหวล่าสุด + ลำดับที่สไลด์ล่าสุด | **MVP** |
| `SessionSummary` → เก็บ `CompletedAllSlides` + `LastSlideObjectId` ซึ่ง **ย้ายไปอยู่ที่ "การเรียน" แล้ว** และเก็บ `UnansweredPoints` ที่คำนวณจาก `SessionQuestion` ที่ `AnswerStatus = not_found` ได้ (§3.5) | **ยังเปิดอยู่ — จะลบทิ้งหรือเก็บไว้ เป็นคำถามเทคนิค ส่งต่อ `system-analyst`** (Q4) |
| env `INACTIVE_THRESHOLD_MINUTES=30` | **MVP** |

## Constraints & Assumptions

- **ไม่ใช่โปรเจกต์ใหม่** — ต่อยอดจากระบบที่ deploy โค้ดจริงอยู่แล้ว (`frontend/` Next.js 15 +
  `backend/` ASP.NET Core .NET 10 + EF Core/PostgreSQL) การเปลี่ยน schema ต้องผ่าน EF Core migration ใหม่
  ตามกฎใน root `CLAUDE.md` — **ไม่ใช่ Prisma** แม้ pipeline default จะพูดถึง `schema.prisma`
- แบบแผน multi-company เดิมต้องคงไว้: ข้อมูลทุกชิ้นของโมดูลนี้ผูกกับบริษัท และต้องกรองด้วย query filter
- ค่า threshold ปรับผ่าน env ตามแบบแผน `ServerDefaults.cs` ของโปรเจกต์ ไม่ hardcode
- **ยังไม่มี auth ในระบบ (TD-002)** — โมดูลนี้ไม่ได้แก้เรื่องนั้น ข้อมูลที่เพิ่มเข้ามาไม่มีข้อมูล
  ส่วนบุคคล (ชื่ออย่างเดียว ตาม F2) แต่ยังมี **หมายเหตุรีวิวภายในของ CS** ที่ไม่ควรเปิดสาธารณะ
  และ `/admin/*` ยังเปิดอยู่ — ให้ `system-analyst`/`project-manager` ชั่งว่าจะติด 🔒 Security gate
  ด้วยเหตุนี้หรือไม่ **แต่ไม่ใช่ด้วยเหตุผลเรื่อง PII ซึ่งถูกตัดออกไปแล้วโดยตั้งใจ**
- ตัดสินใจโดยตั้งใจ (อย่าเสนอเปลี่ยนโดยไม่ amend เอกสารนี้ก่อน):
  - **1 ลิงก์ = หลายการเรียน แยกคนละคน** ตามสเปกเดิม §1 — ยืนยันซ้ำเมื่อ 2026-08-18
    หลังเคยพลิกเป็น 1:1 แล้วเพิกถอนในวันเดียวกัน
  - ลิงก์เป็นสื่อการสอนของกลาง แจกในกลุ่มไลน์ได้ · การเรียนเกิดตอนกดเข้า+ระบุชื่อ
  - `LearnerKey` ใน browser คือตัวแยกคนบนลิงก์เดียวกัน (F3) — ล้าง storage/เปลี่ยนเครื่อง =
    กลายเป็นคนใหม่ ยอมรับแล้ว รวมถึงกรณีที่ CS แยกสองรายการไม่ออก (เหมือนกรณีชื่อซ้ำ)
  - **มีกุญแจแล้วเจอการเรียนค้างอยู่ = ต้องถามยืนยันก่อนเสมอ ห้าม resume เงียบๆ** (F3 กรณี ข) —
    เพราะกุญแจอยู่ที่เบราว์เซอร์ ไม่ใช่ที่คน และเครื่องกลางในโรงเรียน/ห้องสมุดถูกใช้ร่วมกัน ·
    ต้นทุน = กดยืนยันเพิ่มหนึ่งครั้งตอนกลับเข้ามา ยอมรับแล้ว · ห้ามแก้เป็น login/OTP
  - "หยุดกลางคัน" คำนวณตอนแสดงผล ไม่เก็บสถานะลง DB
  - "ครบทุกสไลด์" กับ "จบแล้ว" เป็นคนละค่า ห้ามยุบรวม
  - หมายเหตุรีวิวเป็นข้อความอิสระ ไม่ใช่ enum/dropdown
  - "จุดที่ AI ตอบไม่ได้" ไม่เก็บซ้ำ คำนวณจาก `AnswerStatus = not_found`

## Open Questions

> **คำถามธุรกิจถูกตอบครบแล้วเมื่อ 2026-08-18** — Q1, Q5, Q6, Q7, Q8 ปิดแล้ว คำตอบถูกเขียน
> ลงเป็นกติกาในเนื้อเอกสาร (F1–F8) ไม่ใช่ค้างเป็นคำถาม · เหลือ **Q2–Q4** ซึ่งเป็นคำถามเชิงเทคนิค
> ที่ `system-analyst` ตัดสินร่วมกับผู้ใช้ — **ทั้งสามข้อเปิดเต็มตัว** ไม่มีข้อไหน "แทบตกไปเอง"

| # | คำถาม | ใครตอบ | สถานะ |
|---|---|---|---|
| Q2 | จะ rename `TrainingSession` ให้สื่อว่าเป็น "ลิงก์" ไหม | `system-analyst` + ผู้ใช้ | **เปิดอยู่ และสำคัญจริง** — เมื่อแยกลิงก์ออกจากการเรียนแล้ว ชื่อ `TrainingSession` จะไม่ตรงความหมายอีกต่อไป (มันคือ "ลิงก์" ไม่ใช่ "การเรียน") อ่านโค้ดผิดได้ง่าย แต่ rename มีต้นทุน migration + สัญญา API |
| Q3 | ตั้งชื่อตารางใหม่ ("การเรียนของแต่ละคน") ว่าอะไร | `system-analyst` + ผู้ใช้ | **เปิดอยู่ และต้องตอบแน่นอน** — มีตารางใหม่แน่แล้ว (F1b) จึงต้องมีชื่อ · ชื่อนี้ผูกกับ Q2 ควรตัดสินคู่กัน |
| Q4 | ลบ `SessionSummary` ทิ้ง หรือเก็บไว้ (มีข้อมูลเดิมค้างอยู่หรือไม่ ต้อง migrate ไหม) | `system-analyst` + ผู้ใช้ | **เปิดอยู่** — ตามสเปกเดิม §3.5: ทั้ง `CompletedAllSlides` และ `LastSlideObjectId` ย้ายไปอยู่ที่ "การเรียน" แล้ว ส่วน `UnansweredPoints` คำนวณจาก `SessionQuestion` ที่ `AnswerStatus = not_found` ได้ ทำให้ตารางนี้ซ้ำซ้อนทั้งใบ แต่การลบ/migrate เป็นการตัดสินของ `system-analyst` |

### 🆕 บันทึกไว้เมื่อ 2026-08-21 — ค่า pacing ของบทเรียนควรกลายเป็น "ค่าเริ่มต้นระดับบริษัท" · ✅ ตอบครบแล้ว 2026-08-22 · ⚠️ **P1 ถูกกลับคำตอบภายหลังในวันเดียวกัน**

> ⚠️ **อ่านก่อน:** คำตอบ **P1** ในตารางด้านล่าง (บทเรียน override ได้ · `null` = สืบทอด)
> **ถูกกลับคำตอบโดยเจ้าของโปรเจกต์เองในรอบที่สองของวันที่ 2026-08-22** — ดูหัวข้อย่อย
> "🔄 กลับคำตอบ P1 เมื่อ 2026-08-22 (รอบที่สอง)" ที่ท้ายหัวข้อนี้ · **P2–P5 ยังมีผลบังคับตามเดิม**
> (ยกเว้น P4 ที่ถูกแทนที่ด้วยมติใหม่เรื่องการทิ้งค่า override เดิม — ระบุไว้ในหัวข้อย่อยนั้น)

> **สถานะ (อัปเดต 2026-08-22): สัมภาษณ์เจ้าของโปรเจกต์แล้ว P1–P5 ปิดครบ**
> เดิมบันทึกไว้เมื่อ 2026-08-21 เป็น "ความสนใจ ยังไม่ตัดสิน" — ตอนนี้มีคำตอบจริงจากเจ้าของโปรเจกต์
> ครบทุกข้อแล้ว (ดูตาราง P1–P5 ด้านล่าง) · **พร้อมส่งต่อ `system-analyst` เพื่อปิด A5/B3/B4
> ใน `_docs/module/company-admin/design.md`** · **ยังห้าม engineer หยิบไปทำเอง** — ต้องรอ
> `system-analyst` เคาะรูปร่าง schema (B4) ก่อน
>
> ที่มาเดิม: เจ้าของโปรเจกต์ยกประเด็นขึ้นระหว่างรีวิวหน้าแก้บทเรียน
> (`frontend/src/app/admin/lessons/[slug]/page.tsx`) ว่าฟอร์มมีช่องกรอกตัวเลขรกเกินจำเป็น

**ข้อเท็จจริงที่ตรวจกับโค้ดจริงแล้วในรอบนี้ (ไม่ใช่การอนุมาน):**

1. `introWaitMs` / `breathPauseMs` / `finalQuestionWaitMs` (ค่าระดับทั้งบทเรียน)
   **ขับพฤติกรรมจริง ไม่ซ้ำซ้อน** — ตรวจที่ `frontend/src/tutor/tutor-reducer.ts`:
   `introWaitMs` = timeout ที่ auto-continue ตอนเข้าสถานะ `ready` · `breathPauseMs` = ช่วงหยุด
   หายใจระหว่างสไลด์ · `finalQuestionWaitMs` = ช่วงเปิดให้ถามคำถามสุดท้ายก่อนปิดการเรียนอัตโนมัติ ·
   **สมมติฐานแรกของเจ้าของโปรเจกต์ที่ว่า "สไลด์จบเมื่อเสียงจบอยู่แล้ว จึงไม่ต้องมีค่าพวกนี้"
   ไม่ถูกต้อง** — ตัดทิ้งไม่ได้ ต้องย้ายที่ตั้งค่าเท่านั้น
2. `videoDurationMs` (ค่าระดับต่อสไลด์) — ตามสูตรใน `frontend/docs/STATE_MACHINE.md:60`
   (`เวลา slide = max(0, videoDurationMs - actualTtsMs) + breathPauseMs`) ค่านี้เป็น **no-op (0)**
   สำหรับสไลด์ที่ไม่มีวิดีโอฝังอยู่ และมีความหมายเฉพาะสไลด์ที่มีวิดีโอจริงเท่านั้น

**สิ่งที่เจ้าของโปรเจกต์ขอ (หลังได้ข้อมูลข้างต้น):** ย้าย 3 ค่าในข้อ 1 จาก "ตั้งต่อบทเรียน"
ไปเป็น **ค่าเริ่มต้นระดับบริษัท ตั้งครั้งเดียว** แล้วให้บทเรียนสืบทอด แทนที่ทุกบทเรียนต้องกรอก
เลขชุดเดิมซ้ำ

**นี่ไม่ใช่ไอเดียใหม่ — มันคือคำถามที่ค้างอยู่แล้วและถูกเลื่อนไว้แล้ว:**

- ⚠️ **แก้ข้อมูลที่เคยเข้าใจผิด**: คำถาม **A5 · B3 · B4** ที่ต้องอ่านประกอบ **ไม่ได้อยู่ใน
  `learning-session/design.md`** (หัวข้อ Unresolved Open Questions ของโมดูลนี้ปิดครบแล้ว
  ไม่มีข้อ A5/B3/B4 เลย) — อยู่ที่ **`_docs/module/company-admin/design.md` §Unresolved Open
  Questions กลุ่ม "⏸️ เลื่อนไปพร้อม F2"** (ประมาณบรรทัด 590–645) ตรวจไฟล์จริงยืนยันแล้ว 2026-08-21
- **A5** = ขอบเขตค่าที่รับได้ของค่าตั้งค่าระดับบริษัท (วันนี้ครอบ ลิงก์หมดอายุ / ความเร็วพูด /
  สีแบรนด์ / ชื่อแบรนด์) — **ยังไม่ครอบค่า pacing ทั้งสามตัว** ถ้ารับคำขอนี้ A5 ต้องขยายให้ครอบด้วย
- **B3** = "ค่ากลาง" (ค่าที่ `null` แปลว่าไปสืบทอดมาจากไหน) หมายถึงอะไรกันแน่สำหรับค่าที่
  **ไม่มีค่ากลางอยู่ในระบบวันนี้** — pacing อยู่ในกลุ่มเดียวกันนี้ (วันนี้ค่าอยู่ที่บทเรียนล้วน)
- **B4** = รูปร่าง schema ของค่าตั้งค่าระดับบริษัท: เพิ่ม nullable column ลง `Company` ตรงๆ (ก)
  vs ตารางใหม่ `CompanySetting` (ข) vs ตารางใหม่ + query filter (ค) — `company-admin/design.md`
  ระบุเองว่า **นี่คือข้อที่บล็อกหนักที่สุด ตอบข้อเดียวปลดล็อกทั้ง Data Model**

**สิ่งที่บันทึกไว้รอบนี้คือ: เจ้าของโปรเจกต์แสดงความสนใจโดยตรงที่จะ resolve A5/B3/B4
โดยมี "ค่าเริ่มต้น pacing ของบทเรียน" เป็นกรณีใช้งานที่ผลักดัน** — ไม่ใช่การตอบคำถามเหล่านั้น

**✅ คำตอบจากการสัมภาษณ์เจ้าของโปรเจกต์ 2026-08-22 (ยืนยันแล้ว ไม่ใช่ข้อเสนอ ไม่ใช่การเดา):**

| # | คำถาม | คำตอบที่เคาะแล้ว |
|---|---|---|
| P1 | ~~ถ้าบริษัทตั้งค่ากลางแล้ว บทเรียนบางบทยัง override เองได้ไหม~~ ⛔ **ถูกกลับคำตอบแล้ว 2026-08-22 (รอบที่สอง) — ห้ามใช้คำตอบในช่องขวาเป็นข้อมูลอ้างอิงอีกต่อไป ดูหัวข้อย่อย "🔄 กลับคำตอบ P1"** | ~~**ได้** — ค่าที่ระดับบทเรียนเป็น nullable · `null` = สืบทอดค่าจากบริษัท · **ฟอร์มบทเรียนยังคงมี 3 ช่องนี้อยู่ ปล่อยว่างได้** (ยอมรับว่าความรกของฟอร์มไม่ได้หายไปทั้งหมด แลกกับความยืดหยุ่นของบทเรียนที่ต้องการจังหวะพิเศษ)~~ |
| P2 | ค่าเริ่มต้นที่ถูกต้องคือชุดไหน | **ยึดค่าจาก server env (`ServerDefaults.cs`) เป็นค่าจริง: `introWaitMs = 5000`, `breathPauseMs = 500`, `finalQuestionWaitMs = 5000`** · อีกสองชุดที่อยู่ในโค้ดวันนี้ **เพี้ยน ต้องแก้ให้ตรงกัน** (ดูตารางเทียบด้านล่าง) |
| P3 | บล็อกกับหน้าตั้งค่าบริษัท (F2 ของ `company-admin`) ไหม | **ไม่บล็อก — ลง schema/API ระดับบริษัทได้เลยตอนนี้** ไม่ต้องรอหน้า UI ตั้งค่าบริษัท (F2) เสร็จก่อน |
| P4 | ค่าที่บทเรียนเดิมกรอกไว้แล้วจะเกิดอะไรขึ้นตอนย้าย | **Backfill เป็น override ทุกบทเรียน** — บทเรียนที่มีอยู่แล้วทุกบทได้ค่าเดิมของตัวเองเป็น override ที่ระดับบทเรียน **ไม่มีบทไหนถูกเปลี่ยนเป็น `null` อัตโนมัติ** (พฤติกรรมของบทเรียนเดิมต้องไม่เปลี่ยนหลัง migration) |
| P5 | *(คำถามใหม่ที่เกิดจาก P1 + P3 รวมกัน)* ตอนสร้างบริษัทใหม่ ค่าระดับบริษัทมาจากไหน | **Backfill จาก server env ทันทีตอนสร้างบริษัท — ไม่มี `null` ที่ชั้นบริษัทเลย** |

**⚠️ ค่า default วันนี้มี 3 ชุดในโค้ด และไม่ตรงกัน — นี่คืองานที่ตามมาจากรอบนี้ ไม่ใช่แค่การย้ายที่ตั้งค่า:**

| ที่มาในโค้ด | `introWaitMs` | `breathPauseMs` | `finalQuestionWaitMs` | สถานะ |
|---|---|---|---|---|
| server env — `ServerDefaults.cs` | 5000 | 500 | 5000 | ✅ **ค่าที่ถูกต้อง (เจ้าของโปรเจกต์เคาะ)** |
| ฟอร์มสร้างบทเรียนใหม่ — `frontend/src/app/admin/lessons/new/page.tsx` | 3000 | 800 | 5000 | ❌ เพี้ยน — ต้องแก้ให้ตรง server env |
| fallback ตอนเล่นจริง — `frontend/src/hooks/use-tutor-session.ts` | 5000 | 1000 | 5000 | ❌ เพี้ยน — ต้องแก้ให้ตรง server env |

งานที่ตามมา: **แก้ 2 จุดที่เพี้ยนให้ตรงกับ server env** ควบคู่กับการย้ายค่าไปตั้งที่ระดับบริษัท —
ถ้าย้ายอย่างเดียวโดยไม่แก้ ความไม่ตรงกันนี้จะถูกยกไปทั้งก้อน

**โมเดลการสืบทอดที่เคาะแล้ว — มี 2 ชั้นเท่านั้น:**

```
บทเรียน (nullable — override ได้, null = สืบทอด)
   ↓ ถ้า null
บริษัท (ตั้งค่าเสมอตอนสร้างบริษัท จาก server env — ไม่มี null ได้เลย)
```

**ไม่มีชั้นที่สาม** — เมื่อมีค่าระดับบริษัทแล้ว **ไม่มี fallback ไป env ตอน runtime อีกต่อไป**
(env ถูกใช้ครั้งเดียวคือตอน seed ค่าให้บริษัทใหม่)

**➡️ พร้อมส่งต่อ `system-analyst` แล้ว** เพื่อปิด **A5 / B3 / B4** ใน
`_docs/module/company-admin/design.md` §Unresolved Open Questions กลุ่ม "⏸️ เลื่อนไปพร้อม F2" ·
**ข้อควรเตือน `system-analyst` เรื่อง B4 (รูปร่าง schema — nullable column บน `Company` ตรงๆ /
ตารางใหม่ `CompanySetting` / ตารางใหม่ + query filter):** ข้อจำกัดใหม่จากรอบนี้คือ **ค่า pacing
ที่ระดับบริษัทต้องไม่เป็น `null` ได้เลย** ซึ่ง **ต่างจาก field อื่นๆ ของ Module B ที่ `null` =
inherit จาก env** — สามตัวนี้จึงมีกฎการ null คนละแบบกับเพื่อนร่วมกลุ่ม อย่าออกแบบรวมเป็นกฎเดียว
โดยไม่ตั้งใจ · **คำขอนี้เป็นของโมดูล `company-admin` โดยสภาพ** (มันคือค่าตั้งค่าระดับบริษัท)
บันทึกไว้ที่นี่เพราะพฤติกรรมที่ค่าพวกนี้ขับอยู่เป็นของ `learning-session`

**แยกออกจากกันชัดๆ — ข้อ 2 (`videoDurationMs`) ไม่เกี่ยวกับรอบนี้และไม่ต้องผ่านกระบวนการนี้**
(การสัมภาษณ์ P1–P5 เมื่อ 2026-08-22 ครอบเฉพาะ 3 ค่าในข้อ 1 เท่านั้น ไม่แตะ `videoDurationMs`):
มันเป็น
**การลดความรกของ UI ล้วน ไม่ใช่การตัดสินใจเชิงธุรกิจ** — ข้อเสนอคือแสดงช่องนี้เฉพาะสไลด์ที่มี
วิดีโอจริง แทนที่จะเป็นช่องตัวเลข default 0 บนทุกสไลด์ · **ไม่เปลี่ยน schema ไม่เปลี่ยนพฤติกรรม
ของ reducer** ส่งเป็นงาน `frontend-engineer` ได้ตรงๆ เมื่อเจ้าของโปรเจกต์สั่ง ไม่ต้องรอ A5/B3/B4

#### 🔄 กลับคำตอบ P1 เมื่อ 2026-08-22 (รอบที่สองของวันเดียวกัน) — ✅ ยืนยันแล้ว ไม่ใช่ข้อเสนอ

**สิ่งที่ถูกกลับ:** คำตอบ **P1** ที่ปิดไปเมื่อ 2026-08-22 (รอบแรก) ว่า *"บทเรียนยัง override
ค่ากลางเองได้ · `null` = สืบทอด · ฟอร์มบทเรียนยังคงมี 3 ช่องนี้อยู่"* — **ยกเลิกทั้งข้อ**
เจ้าของโปรเจกต์เปลี่ยนไปเลือก **ตัวเลือก ข.** ที่เคยเสนอคู่กันไว้ตั้งแต่รอบแรก
หลังได้เห็นหน้าจอจริงที่ implement ไปแล้ว

**คำพูดต้นฉบับของเจ้าของโปรเจกต์ (เหตุผลของการกลับคำตอบ — คัดคำต่อคำ):**

> "ส่วนที่ย้ายไปแล้วก็ควรเคลียร์ทิ้ง เพราะมันซ้ำ จำเจ จุดประสงค์ที่ย้ายออกไปเป็นของรวม
> คือให้เป็น UX เดียวกันทั้งระบบ ไม่ต้องให้ซับซ้อนโดยใช่เหตุ"

กล่าวคือเหตุผลคือ **ความสม่ำเสมอของ UX ทั้งระบบ + ตัดความซับซ้อนที่ไม่จำเป็น** ไม่ใช่ปัญหาเชิงเทคนิค —
เมื่อค่าถูกยกไปเป็นของกลางระดับบริษัทแล้ว การคงช่อง override ต่อบทเรียนไว้ทำให้เกิดจุดตั้งค่าสองที่
ซึ่งขัดกับจุดประสงค์ของการย้ายตั้งแต่แรก

**มติใหม่ที่ยืนยันแล้ว 3 ข้อ (ไม่ใช่ข้อเสนอ — เจ้าของโปรเจกต์ตอบตรงในแชท 2026-08-22):**

| # | เรื่อง | มติ |
|---|---|---|
| N1 | ช่องกรอกค่า pacing ในฟอร์มบทเรียน | **ตัดออกถาวร** — `introWaitMs` / `breathPauseMs` / `finalQuestionWaitMs` ไม่มีในฟอร์มบทเรียนอีกต่อไป **บังคับใช้ค่ากลางของบริษัทล้วนทุกบทเรียน ไม่มี override ต่อบทเรียนอีกต่อไป** |
| N2 | ค่า override ที่บทเรียนเดิมมีอยู่แล้ว (เช่นบทที่เป็น 3000/500/3000) | **ทิ้งทั้งหมด** — ไม่เก็บไว้ ไม่ archive ทุกบทเรียนใช้ค่ากลางของบริษัทล้วน · **มตินี้แทนที่ P4 เดิม** ("backfill เป็น override ทุกบทเรียน") ซึ่งหมดผลบังคับแล้ว · ยอมรับโดยเจ้าของโปรเจกต์ว่าบทเรียนเดิมบางบทจะมีจังหวะเปลี่ยนไปหลัง migration |
| N3 | ชะตากรรมของคอลัมน์ในฐานข้อมูล | **ลบคอลัมน์ออกจากฐานข้อมูลจริง** ไม่ใช่แค่ซ่อนจากหน้าจอ และไม่ใช่ปล่อยค้างเป็น nullable ที่ไม่มีใครใช้ — ต้องการให้สะอาดครบทั้ง schema |

**โมเดลการสืบทอดหลังกลับคำตอบ — เหลือชั้นเดียว:**

```
บริษัท (ตั้งค่าเสมอตอนสร้างบริษัท จาก server env — ไม่มี null ได้เลย)
   ↓ ใช้ตรงๆ ทุกบทเรียน ไม่มีชั้น override
```

**สิ่งที่ยังมีผลบังคับตามเดิม ไม่ถูกกลับ:** P2 (ค่าที่ถูกต้องคือ server env 5000/500/5000 และ
2 จุดที่เพี้ยนต้องแก้ให้ตรง) · P3 (ไม่บล็อกกับ F2) · P5 (seed ค่าบริษัทจาก env ตอนสร้างบริษัท)

**⚠️ ผลกระทบต่องานที่ implement ไปแล้ว — นี่ไม่ใช่การแก้แค่ไฟล์นี้:**

งาน pacing ตามคำตอบ P1 **เดิม** ถูก implement ไปแล้วทั้ง **Phase 4 (backend) และ Phase 5
(frontend) ของโมดูล `company-admin`** และ **ยังไม่เคยผ่าน QA เลยสักรอบ** — จังหวะนี้จึงเป็น
จุดที่ถูกที่สุดที่จะกลับคำตอบ แต่ **โค้ดที่เขียนไปแล้วตามสัญญาเดิมต้องถูกถอด ไม่ใช่ปล่อยไว้**
สิ่งที่ต้องถูก amend ตามใน `_docs/module/company-admin/design.md` (⛔ **`business-analyst`
ไม่แก้ไฟล์นั้นเอง — เป็นงานของ `system-analyst`**):

- **LP-1 .. LP-15** — สัญญาทั้งชุดของ pacing ต้องทบทวนทีละข้อ โดยเฉพาะ
  **LP-6 / LP-11 / LP-12 / LP-13** ที่เขียนไว้เรื่อง **override ต่อบทเรียน และกฎ
  empty-vs-zero** (ช่องว่าง = สืบทอด vs กรอก `0` = ตั้งใจให้เป็นศูนย์) — เมื่อไม่มีช่องกรอก
  ที่ระดับบทเรียนอีกต่อไป **กฎ empty-vs-zero ทั้งก้อนหมดเหตุผลที่จะมีอยู่** ต้องถูกลบ
  ไม่ใช่แค่แก้ถ้อยคำ
- **DM-P2 (LessonConfig nullable columns)** — เปลี่ยนจาก *"เพิ่ม nullable column ลง
  `LessonConfig`"* เป็น ***"ไม่ต้องมีคอลัมน์ pacing ใน `LessonConfig` เลย"*** และต้องมี
  **migration ใหม่สำหรับลบคอลัมน์ที่ migration ก่อนหน้าเพิ่มไว้** (ตาม N3)
- เมื่อ `design.md` ถูก amend แล้ว งาน Phase 4/5 ที่ทำไปตามสัญญาเดิมจะกลายเป็น drift
  → เป็นงานของ `project-manager` ที่จะจัดลำดับการถอดออก แล้วจึงส่ง engineer

### 🆕 บันทึกไว้เมื่อ 2026-08-22 — ฝั่งผู้เรียน: Responsive มือถือ/แท็บเล็ต + พิมพ์ถามแทนการพูด · ⛔ ยังไม่สัมภาษณ์

> **สถานะ: capture-only — บันทึกกันลืมอย่างเดียว ยังไม่มีคำตอบ ยังไม่มีมติ**
> เจ้าของโปรเจกต์ยกสองประเด็นนี้ขึ้นมาระหว่างคุยเรื่องอื่น (กำลังเตรียมส่งสกรีนช็อตให้ทีม UX/UI)
> และ **ขอให้บันทึกไว้ก่อน ยังไม่ได้สัมภาษณ์เต็มรูปแบบ** · ⛔ **ห้าม engineer หยิบไปทำ**
> ⛔ **ห้าม `system-analyst` ออกแบบทับ** จนกว่า `business-analyst` จะสัมภาษณ์จริงและปิดคำถาม
> R1–R? / T1–T? ด้านล่าง · เขียนตามแบบเดียวกับหัวข้อ pacing 2026-08-21 ซึ่งเคยเป็น capture-only
> มาก่อนแล้วค่อยปิดด้วยการสัมภาษณ์จริงในวันถัดมา

**คำพูดต้นฉบับของเจ้าของโปรเจกต์ (คัดคำต่อคำ ห้ามตีความแทนตอนสัมภาษณ์จริง):**

> "แยกกันละกัน ฝั่งผู้เรียนยังไม่ต้องเอา เพราะฉันยังไม่ได้ลงดีเทลอีกนิดหน่อย เดี๋ยวเราต้องไปแก้กัน
> ขอบอกไว้เลยละกัน นายจะได้ส่งต่อให้ BA โน้ตลง เราจะจัดการเรื่องของ Responsive mobile และ แท็บเล็ต
> ฝั่งผู้เรียนด้วย และก็ลงดีเทล usecase ในการพิมพ์ถาม แทนการพูดด้วย เพราะฉันเชื่อว่าต้องมี User
> ที่ไม่สะดวกพูดถามตลอดด้วย (เราต้องคิดทุกการทำงานของ responsive)"

บริบทของคำว่า "แยกกันละกัน / ฝั่งผู้เรียนยังไม่ต้องเอา": เจ้าของโปรเจกต์กำลังจัดชุดงานที่จะส่งให้
ทีม UX/UI และ **กันฝั่งผู้เรียนออกจากรอบนั้นไปก่อน** เพราะยังไม่ได้ลงรายละเอียดเอง —
ไม่ใช่การประกาศว่าฝั่งผู้เรียนถูกตัดออกจากขอบเขต

#### ประเด็นที่ 1 — Responsive มือถือ/แท็บเล็ต ฝั่งผู้เรียน (ห้องเรียน + join flow)

จุดเน้นที่เจ้าของโปรเจกต์พูดเอง: **"เราต้องคิดทุกการทำงานของ responsive"** — คือไม่ใช่แค่จัด CSS
ให้พอดีจอ แต่ต้องไล่ว่า **แต่ละ interaction ที่ทำได้บน desktop จะทำงานยังไงบนมือถือ/แท็บเล็ต**
(เช่น push-to-talk, การดูสไลด์, การเลื่อนดูเนื้อหา ฯลฯ) — รายการนี้เป็นตัวอย่างที่เจ้าของโปรเจกต์
ยกเอง ไม่ใช่รายการที่ครบถ้วนแล้ว

**ข้อเท็จจริงจากโค้ดจริงที่ตรวจแล้ว 2026-08-22 (บันทึกเพื่อให้การสัมภาษณ์ทีหลังไม่ต้องเริ่มจากศูนย์
— ไม่ใช่การเสนอทางแก้):**

- `frontend/src/app/room/[token]/page.tsx` มี breakpoint อยู่บ้างแล้ว (`md:flex-row`, `md:w-72`,
  แถบข้างพับเป็นแนวนอนบนจอเล็ก) และ layout เป็น `h-screen` + `overflow-hidden` — **มี responsive
  ระดับ layout อยู่แล้วบางส่วน แต่ยังไม่เคยมีใครตรวจว่าทุก interaction ใช้งานได้จริงบนจอเล็ก**
- `frontend/src/components/meeting/PushToTalkButton.tsx` ผูก `onMouseDown`/`onMouseUp`/
  `onMouseLeave`/`onTouchStart`/`onTouchEnd` + `onKeyDown` — **มี touch handler อยู่แล้ว**
  แต่ยังไม่มีหลักฐานว่าเคยทดสอบจริงบนมือถือ/แท็บเล็ต (กด-ค้าง-ปล่อยบนจอสัมผัสขณะหน้าเลื่อน
  เป็นพฤติกรรมคนละแบบกับเมาส์)

**คำถามที่ต้องถามตอนสัมภาษณ์จริง (ยังไม่มีคำตอบทั้งหมด — อย่าเดา):**

| # | คำถาม | ทำไมต้องถาม |
|---|---|---|
| R1 | อุปกรณ์/ขนาดจอเป้าหมายคืออะไรบ้าง และตัวไหนคือตัวหลัก | "มือถือ" กับ "แท็บเล็ต" มีข้อจำกัดคนละแบบ (พื้นที่สไลด์, การกดค้าง, แป้นพิมพ์บังจอ) |
| R2 | ฝั่ง CS/หลังบ้าน (`/admin/*`) อยู่ในขอบเขตรอบนี้ด้วยไหม | คำพูดต้นฉบับระบุ "ฝั่งผู้เรียน" อย่างเดียว — ต้องยืนยันว่าหลังบ้านตั้งใจไม่เอาจริง |
| R3 | แต่ละ interaction ในห้องเรียนต้องทำงานยังไงบนจอเล็ก (push-to-talk, ดูสไลด์, เลื่อนเนื้อหา, แชต, ปุ่มจบ, ปุ่มเรียนอีกครั้ง) | นี่คือแกนของ "ต้องคิดทุกการทำงานของ responsive" — ต้องไล่ทีละอันกับเจ้าของโปรเจกต์ |
| R4 | มีการทำงานไหนที่ยอมให้ "ไม่รองรับบนมือถือ" ได้ไหม หรือต้องครบทุกอย่าง | ตัดสินขอบเขต MVP vs Later |
| R5 | จอแนวตั้ง (portrait) ต้องใช้งานได้ หรือบังคับ/แนะนำให้หมุนเป็นแนวนอน | สไลด์เป็นเนื้อหาแนวนอนโดยธรรมชาติ |
| R6 | เกี่ยวข้องกับงาน UX/UI รอบที่กำลังส่งอยู่ไหม หรือรอเป็นรอบถัดไป | คำพูดต้นฉบับบอกว่ากันออกไปก่อน — ต้องรู้ว่าจะกลับเข้ามาเมื่อไหร่ |

#### ประเด็นที่ 2 — พิมพ์ถามแทนการพูด (use case เต็มรูปแบบ ไม่ใช่ปุ่มเสริม)

เหตุผลที่เจ้าของโปรเจกต์ให้: **"ต้องมี User ที่ไม่สะดวกพูดถามตลอด"** (เช่นอยู่ในที่สาธารณะ
ห้องประชุม หรือไม่สะดวกพูดต่อหน้าคนอื่น — ตัวอย่างเหล่านี้เป็นการขยายความ ยังต้องยืนยันกับเจ้าของ
โปรเจกต์เองว่าตรงกับที่คิดไว้ไหม)

**ข้อเท็จจริงจากโค้ดจริงที่ตรวจแล้ว 2026-08-22 — วันนี้ "ถาม AI" ทำได้ทางเสียงทางเดียวจริง:**

1. `frontend/src/lib/api-client.ts:447` `askVoiceQuestion(...)` — จุดเดียวที่คำถามถูกส่งไปหา AI
   (`POST /api/voice-question`) และ **บังคับต้องมี `audioBlob` เสมอ** ไม่มี parameter ที่รับ
   ข้อความล้วน · `frontend/src/hooks/use-tutor-session.ts` อัดเสียงเป็น `Blob` แล้วเรียกฟังก์ชันนี้
2. `frontend/src/components/meeting/ControlBar.tsx` มีปุ่มเดียวสำหรับถาม คือ `PushToTalkButton`
   (คอมเมนต์ในไฟล์เขียนไว้เองว่า "Push-to-Talk is the only way a teacher speaks")
3. **มีช่องพิมพ์อยู่ในห้องจริง แต่คนละเส้นทาง — อย่าสับสน**: `ChatDrawer.tsx:109` มี `<Input>`
   placeholder `"พิมพ์ข้อความ..."` แต่มันส่งผ่าน `use-session-chat.ts:122`
   → SignalR `SendChatMessage` ซึ่งเป็น **แชตข้อความระหว่างคน (ผู้เรียน ↔ CS)** ไม่ได้เข้า
   pipeline ถอดเสียง/RAG/ตอบของ AI เลย · **สรุป: วันนี้ไม่มีทางพิมพ์ถาม AI ได้ ยืนยันกับโค้ดแล้ว**
   — แต่ **ช่องพิมพ์ที่มีอยู่แล้วนี้จะเป็นประเด็นตอนออกแบบ** เพราะผู้เรียนอาจเข้าใจว่าพิมพ์ในนั้น
   แล้ว AI จะตอบ (คำถามข้อ T4)

**คำถามที่ต้องถามตอนสัมภาษณ์จริง (ยังไม่มีคำตอบทั้งหมด — อย่าเดา):**

| # | คำถาม | ทำไมต้องถาม |
|---|---|---|
| T1 | พิมพ์ถามเป็น "ทางเลือกเทียบเท่าการพูด" (ทำได้ทุกอย่างที่พูดได้) หรือเป็นทางสำรองที่จำกัดกว่า | คำพูดต้นฉบับบอกว่า "ลงดีเทล usecase" = น่าจะเทียบเท่า แต่ต้องยืนยันเอง |
| T2 | คำถามที่พิมพ์ต้องถูกบันทึก/แสดง/รีวิวเหมือนคำถามที่พูดไหม (F7 รีวิวถูก/ผิดของ CS, หน้าสรุป F5) | ถ้าใช่ = แตะโครงสร้างข้อมูลคำถาม ไม่ใช่งาน UI ล้วน |
| T3 | คำตอบของ AI ต่อคำถามที่พิมพ์ ยังต้องอ่านออกเสียง (TTS) ไหม หรือแสดงเป็นข้อความอย่างเดียว | คนที่ไม่สะดวก*พูด* อาจไม่สะดวก*ฟัง*ด้วย (ในที่ประชุม) — แต่ห้ามสรุปแทนเจ้าของโปรเจกต์ |
| T4 | ช่องพิมพ์ถาม AI กับช่องแชตหาคนที่มีอยู่แล้วใน `ChatDrawer` จะอยู่ร่วมกันยังไง | สองช่องหน้าตาเหมือนกันแต่ปลายทางคนละที่ = ความสับสนที่คาดเดาได้ |
| T5 | ระหว่างที่ AI กำลังบรรยายอยู่ การพิมพ์ต้องหยุดการบรรยายเหมือนตอนกด push-to-talk ไหม | วันนี้การกดถาม = interrupt state machine · การพิมพ์ไม่มีจังหวะ "กดค้าง/ปล่อย" ให้ยึด |
| T6 | ตอบคำถามยืนยัน "พร้อมหรือยังคะ" (`expecting: "readiness"`) ด้วยการพิมพ์ได้ด้วยไหม | เส้นทางนี้วันนี้ก็ผ่าน `askVoiceQuestion` เหมือนกัน |
| T7 | อยู่ใน MVP ของโมดูลนี้ หรือเป็นงานรอบถัดไปแยกจาก F1–F8 | จัดลำดับงาน — F1–F8 กำลังเดินอยู่แล้ว |

**สองประเด็นนี้เกี่ยวกันแต่ไม่ใช่เรื่องเดียวกัน** — พิมพ์ถามช่วยเรื่องมือถือ (ไม่ต้องกดค้างพูด)
แต่มันเป็น use case ของตัวเองที่มีเหตุผลแยกต่างหาก (ไม่สะดวกพูด แม้อยู่บน desktop)
**ตอนสัมภาษณ์ควรถามแยกกัน อย่ายุบรวมเป็นข้อเดียว**

## Declined / Not Pursuing

### ~~ตัดออกเมื่อ 2026-08-18 — จากการพลิกเป็น "1 ลิงก์ = 1 การเรียน"~~ · ⚠️ เพิกถอนแล้ว 2026-08-18

> **บล็อกนี้ถูกเพิกถอนทั้งบล็อกในวันเดียวกัน (2026-08-18) หลังเจ้าของโปรเจกต์ทบทวน** —
> เก็บไว้เพื่อให้ประวัติการตัดสินใจอ่านย้อนได้ **ไม่ใช่รายการที่มีผลบังคับ**
>
> ลำดับเหตุการณ์ในวันที่ 2026-08-18: (1) เจ้าของโปรเจกต์พลิกจาก "1 ลิงก์ = หลายการเรียน"
> เป็น "1 ลิงก์ = 1 การเรียน" → เกิดรายการ 5 แถวข้างล่างนี้ · (2) ทบทวนอีกครั้งในวันเดียวกัน
> แล้ว **ยกเลิกการพลิก** กลับไปใช้หลักการเดิมของสเปก 2026-08-11 → **ทั้ง 5 แถวข้างล่างกลับมาเป็น
> งานใน MVP** ตามที่เขียนไว้ใน `## Core Features` และ `## Scope`
>
> เหตุผลที่ยกเลิก: use case จริงคือ CS แจกลิงก์เดียวลงกลุ่มไลน์ แล้วหลายคนหยิบไปเรียนแยกกัน
> โครงสร้าง 1:1 รองรับไม่ได้

| ~~สิ่งที่ตัด~~ (เพิกถอนแล้ว — กลับมาทำ) | วันที่ตัด | วันที่เพิกถอน | สถานะปัจจุบัน |
|---|---|---|---|
| แยก "ลิงก์เชิญ" ออกจาก "การเรียน" เป็นสองสิ่ง (สเปกเดิม §1) | 2026-08-18 | 2026-08-18 | **กลับมาเป็น MVP** — F1 |
| ย้าย `RecipientName`/`Status`/`StartedAt`/`EndedAt`/`CompletedAllSlides`/`LastSlideObjectId` ออกจาก `TrainingSession` (§3.1, §3.2) | 2026-08-18 | 2026-08-18 | **กลับมาเป็น MVP** — F1 / F1b |
| `MaxAttendees` และการจำกัดจำนวนคนต่อลิงก์ทั้งหมด (§3.1, §6) | 2026-08-18 | 2026-08-18 | **ช่องกรอกกลับมาเป็น MVP** — F8 · ส่วน *การบังคับใช้* ยังคง Declined ตามมติ 2026-08-11 ในบล็อกถัดไป |
| ปุ่ม "เรียนอีกครั้ง" และการมีหลายรอบต่อคน (§2.5) | 2026-08-18 | 2026-08-18 | **กลับมาเป็น MVP** — F5 |
| หน้าที่ข้อ 2 ของ `LearnerKey` — "แยกว่าใครเป็นใครบนลิงก์เดียวกัน" (§2.4) | 2026-08-18 | 2026-08-18 | **กลับมาเป็น MVP** — F3 (ทำสองหน้าที่ตามสเปกเดิม) |

### ตัดออกเมื่อ 2026-08-11 — §6 ของสเปกเดิม (มีผลบังคับทั้งหมด)

รายการนี้คือ §6 "ที่ตัดออกโดยตั้งใจ (อย่าเผลอทำ)" ของสเปกเดิม — ตัดสินโดยเจ้าของโปรเจกต์เอง
เมื่อ 2026-08-11 (ไม่ใช่ผลจากการวิเคราะห์ feasibility ของ `system-analyst`) **อย่าเผลอทำในเฟสนี้**
ถ้าจะทำต้อง amend เอกสารนี้ก่อน

| สิ่งที่ตัด | วันที่ | เหตุผล | แหล่งที่มาการตัดสิน |
|---|---|---|---|
| Event log ต่อสไลด์ | 2026-08-11 | เพิ่ม *ตาราง* ใหม่ทีหลังไม่เจ็บ (ต่างจากเพิ่ม *คอลัมน์*) และยังไม่มีคนใช้ข้อมูลนี้ | เจ้าของโปรเจกต์ (`CORE_FEATURE_SPEC.md` §2.3, §6) |
| สถานะ "จัดการแล้ว" ในการรีวิว | 2026-08-11 | รอจนรายการกองจน CS สับสนก่อนค่อยเพิ่ม | เจ้าของโปรเจกต์ (§2.7, §6) |
| ตั้งค่า label "หน่วยงาน" ต่อบริษัท | 2026-08-11 | ต้องสร้างตาราง `Company` เพื่อคำเดียว — รอทำพร้อม auth | เจ้าของโปรเจกต์ (§2.1, §6) |
| บังคับใช้ "จำกัดจำนวนคนต่อลิงก์" | 2026-08-11 | เตรียมที่เก็บค่า + ช่องให้ CS กรอกไว้ก่อน แต่ยัง **ไม่บังคับใช้** ในเฟสนี้ — สิ่งที่ตัดคือ *การบังคับใช้* ไม่ใช่ตัวช่องกรอก (ช่องกรอกอยู่ใน MVP ตาม F8 พร้อมข้อความกำกับใน UI ว่าค่ายังไม่มีผล) | เจ้าของโปรเจกต์ (§3.1, §6) |
| ห้องกลุ่ม (หลายคนเรียนพร้อมกัน) | 2026-08-11 | เป็นฟีเจอร์ใหม่ ลิงก์คนละชนิด ไม่ยัดเข้ามาในโครงสร้างนี้ | เจ้าของโปรเจกต์ (§1, §6) |
| ให้ browser ส่งสัญญาณตอนปิดแท็บเพื่อบอกว่าหยุดเรียน | 2026-08-11 | สัญญาณพลาดบ่อย (ปิดคอม/เน็ตหลุด/แบตหมด) — ใช้วิธีคำนวณจากเวลาแทน | เจ้าของโปรเจกต์ (§2.6) |

## References

| หัวข้อ | แหล่งที่มา | วันที่ | หมายเหตุ |
|---|---|---|---|
| สเปกฟีเจอร์ตั้งต้นของโมดูลนี้ (flow, กติกา, ของที่ตัดออก) | `docs/CORE_FEATURE_SPEC.md` — เจ้าของโปรเจกต์เคาะกันเอง | 2026-08-11 | เอกสารภายใน ไม่ใช่ข้อมูลภายนอก · **§1 ตรงกับเอกสารนี้** เอกสารนั้นเก็บไว้เป็นประวัติ ไม่แก้ ให้ยึด `requirement.md` เมื่อขัดกัน |
| use case จริง: ลิงก์ = สื่อการสอนที่ส่งลงกลุ่มไลน์ · ห้องเรียนเกิดตอนกดเข้า+ระบุชื่อ · แต่ละคนเรียน 1:1 | เจ้าของโปรเจกต์อธิบายโดยตรง | 2026-08-18 | เหตุผลตั้งต้นของทั้งโมดูล ยกไปไว้ใน `## Overview` |
| การเพิกถอนการพลิกเป็น "1 ลิงก์ = 1 การเรียน" (กลับไปใช้สเปกเดิม §1) | เจ้าของโปรเจกต์ทบทวนและตัดสินโดยตรง | 2026-08-18 | เกิดวันเดียวกับการพลิก — การพลิกไม่เคยมีผลบังคับจริง |
| คำตอบ Q1, Q5, Q6, Q7, Q8 | เจ้าของโปรเจกต์ตอบโดยตรง | 2026-08-18 | การตัดสินใจทางธุรกิจของเจ้าของเอง ไม่ใช่ข้อมูลภายนอก · Q6/Q7 ถูกแก้คำตอบพร้อมการเพิกถอน |
| โครงสร้าง `TrainingSession` ที่มีอยู่จริง (13 ฟิลด์ + audit) และ `SessionSummary` เก็บข้อมูลซ้ำ | อ่าน `backend/src/SupportRoom.Domain/Entities/TrainingSession.cs` และ `SessionSummary.cs` โดยตรง | 2026-08-18 | ยืนยันแล้ว ไม่ใช่การอนุมาน · ภายใต้หลักการ "1 ลิงก์ = หลายการเรียน" ฟิลด์เหล่านี้คือของที่ต้องย้าย ไม่ใช่ของที่พร้อมใช้ |
| `RecipientName` วันนี้ถูกกรอกโดย "คนสร้างลิงก์" | คอมเมนต์ XML doc บน `TrainingSession.RecipientName` | 2026-08-18 | เป็นเหตุผลว่าทำไม F2 คือการ *ย้ายจุดกรอก* ไม่ใช่เพิ่มฟิลด์ |
| `INACTIVE_THRESHOLD_MINUTES = 30 นาที` | เจ้าของโปรเจกต์กำหนดเอง (`CORE_FEATURE_SPEC.md` §2.6, §4) | 2026-08-11 | เป็นค่าที่ตั้งเอง ปรับผ่าน env ได้ ไม่ใช่ตัวเลขอ้างอิงจากภายนอก |
| สถานะโค้ดจริง: ยังไม่มี `LastActivityAt`, `LastSlideIndex`, env `INACTIVE_THRESHOLD_MINUTES`, `ReviewResult`/`ReviewNote`/`ReviewedAt`, หน้ากรอกชื่อ, ปุ่มกดจบเอง, หน้ารีวิว CS | อ่านจาก `backend/src/SupportRoom.Domain/Entities/` โดยตรง | 2026-08-18 | ยืนยันแล้ว ไม่ใช่การอนุมาน — นี่คือขอบเขตงานจริงที่เหลือ |
| ยังไม่มี auth / `/admin/*` เปิดสาธารณะ (TD-002) | root `CLAUDE.md` — Known Baseline Issues | 2026-08-18 | ข้อจำกัดของระบบเดิม |
| คำตอบ P1–P5 ของค่า pacing (override ได้ / ยึด server env / ไม่รอ F2 / backfill เป็น override / seed ค่าบริษัทจาก env) | เจ้าของโปรเจกต์ตอบโดยตรงในแชท | 2026-08-22 | การตัดสินใจทางธุรกิจของเจ้าของเอง ไม่ใช่ข้อมูลภายนอก |
| ค่า default 3 ชุดที่ไม่ตรงกันในโค้ด (5000/500/5000 · 3000/800/5000 · 5000/1000/5000) | อ่าน `ServerDefaults.cs`, `frontend/src/app/admin/lessons/new/page.tsx`, `frontend/src/hooks/use-tutor-session.ts` โดยตรง | 2026-08-22 | ยืนยันกับโค้ดจริง ไม่ใช่การอนุมาน · ชุดที่ถูกต้องคือ server env ตามมติ P2 |
| การกลับคำตอบ P1 (N1 ตัดช่อง pacing ออกจากฟอร์มบทเรียนถาวร · N2 ทิ้งค่า override เดิมทั้งหมด · N3 ลบคอลัมน์ออกจาก DB จริง) | เจ้าของโปรเจกต์ตอบโดยตรงในแชท หลังเห็นหน้าจอจริงที่ implement ไปแล้ว | 2026-08-22 *(รอบที่สองของวัน)* | การตัดสินใจทางธุรกิจของเจ้าของเอง ไม่ใช่ข้อมูลภายนอก · เหตุผลคือ UX consistency + ลดความซับซ้อนที่ไม่จำเป็น (คำพูดต้นฉบับคัดไว้ในหัวข้อ 🔄) · **แทนที่แถว "คำตอบ P1–P5" ด้านบนเฉพาะส่วน P1 และ P4** |
| คำขอ 2 ข้อของฝั่งผู้เรียน (responsive มือถือ/แท็บเล็ต · พิมพ์ถามแทนพูด) | เจ้าของโปรเจกต์พูดโดยตรงในแชท (คัดคำต่อคำไว้ในหัวข้อ 🆕 2026-08-22) | 2026-08-22 | **ยังเป็นคำขอ ไม่ใช่ requirement** — ยังไม่สัมภาษณ์ ยังไม่มีมติ · เหตุผล "ต้องมี User ที่ไม่สะดวกพูดถามตลอด" เป็นความเชื่อของเจ้าของโปรเจกต์เอง ไม่ได้อ้างอิงข้อมูลภายนอก |
| วันนี้ถาม AI ได้ทางเสียงทางเดียว (`askVoiceQuestion` บังคับมี `audioBlob`) และช่องพิมพ์ใน `ChatDrawer` ไปที่ SignalR `SendChatMessage` ไม่ใช่ AI | อ่าน `frontend/src/lib/api-client.ts:447`, `frontend/src/components/meeting/ControlBar.tsx`, `frontend/src/components/meeting/ChatDrawer.tsx:109`, `frontend/src/hooks/use-session-chat.ts:122` โดยตรง | 2026-08-22 | ยืนยันกับโค้ดจริง ไม่ใช่การอนุมาน |
| สถานะ responsive วันนี้: `room/[token]/page.tsx` มี breakpoint `md:` อยู่บ้าง · `PushToTalkButton` มี `onTouchStart`/`onTouchEnd` อยู่แล้ว | อ่าน `frontend/src/app/room/[token]/page.tsx`, `frontend/src/components/meeting/PushToTalkButton.tsx` โดยตรง | 2026-08-22 | ยืนยันกับโค้ดจริง · **ไม่มีหลักฐานว่าเคยทดสอบจริงบนมือถือ/แท็บเล็ต** |
| ช่องโหว่ "เครื่องใช้ร่วมกัน" ของ `LearnerKey` (คนที่สองถูกพาเข้าไปเรียนต่อของคนแรก) และมติให้ถามยืนยันก่อน resume | `system-analyst` วิเคราะห์พบ → เจ้าของโปรเจกต์เคาะทางแก้โดยตรง | 2026-08-18 | การตัดสินใจทางธุรกิจของเจ้าของเอง ไม่ใช่ข้อมูลภายนอก · เขียนเป็นกติกาไว้ที่ F3 กรณี ข |

## Change Log

- 2026-08-22 *(รอบที่สองของวันเดียวกัน — เจ้าของโปรเจกต์ยืนยันเองในแชทหลังเห็นหน้าจอจริง)* —
  **🔄 กลับคำตอบ P1 ของหัวข้อ pacing** จาก "บทเรียน override ค่ากลางเองได้ (`null` = สืบทอด)"
  เป็น **ตัดช่องกรอกค่า pacing ออกจากฟอร์มบทเรียนถาวร บังคับใช้ค่ากลางของบริษัทล้วนทุกบทเรียน
  ไม่มี override ต่อบทเรียนอีกต่อไป (N1)** · เพิ่มมติใหม่อีก 2 ข้อ: **ทิ้งค่า override เดิมของ
  บทเรียนที่มีอยู่แล้วทั้งหมด (N2 — แทนที่ P4 เดิมที่ให้ backfill เป็น override)** และ
  **ลบคอลัมน์ pacing ออกจากฐานข้อมูลจริง ไม่ใช่แค่ซ่อนจากหน้าจอ (N3)** · เหตุผลตามคำพูดต้นฉบับ:
  ให้เป็น **UX เดียวกันทั้งระบบ ไม่ซับซ้อนโดยใช่เหตุ** เพราะส่วนที่ย้ายไปเป็นของกลางแล้วควรเคลียร์ทิ้ง ·
  P2 / P3 / P5 ยังมีผลบังคับตามเดิม · **กระทบงานที่ implement ไปแล้วแต่ยังไม่เคยผ่าน QA:
  Phase 4 (backend) และ Phase 5 (frontend) ของ `company-admin`** — **LP-1..LP-15 โดยเฉพาะ
  LP-6 / LP-11 / LP-12 / LP-13 (override + กฎ empty-vs-zero) และ DM-P2 (LessonConfig nullable
  columns) ใน `company-admin/design.md` ต้องถูก amend ตาม** พร้อม migration ใหม่สำหรับลบคอลัมน์ ·
  ➡️ **ส่งต่อ `system-analyst`** · ⛔ `business-analyst` ไม่แก้ `design.md`/`plan.md` เอง
  ⛔ ไม่มีโค้ดถูกแตะในรอบนี้
- 2026-08-22 *(วันที่เจ้าของโปรเจกต์ยกประเด็นเองในแชท)* — **บันทึก (ไม่ใช่ตัดสิน) คำขอ 2 ข้อของ
  ฝั่งผู้เรียน: (1) Responsive มือถือ/แท็บเล็ต แบบ "ต้องคิดทุกการทำงาน" ไม่ใช่แค่จัด CSS ·
  (2) พิมพ์ถามแทนการพูด เป็น use case เต็มรูปแบบ** · เพิ่มหัวข้อ "🆕 บันทึกไว้เมื่อ 2026-08-22"
  ใน `## Open Questions` พร้อมคำพูดต้นฉบับคำต่อคำ, ข้อเท็จจริงที่ตรวจกับโค้ดจริง
  (`askVoiceQuestion` บังคับมี `audioBlob` · `ControlBar` มีแต่ push-to-talk · ช่องพิมพ์ใน
  `ChatDrawer` ไปที่ SignalR `SendChatMessage` ไม่ใช่ AI · `PushToTalkButton` มี touch handler
  แล้ว · `room/[token]/page.tsx` มี breakpoint `md:` บ้างแล้ว) และคำถามที่ยังไม่มีคำตอบ
  R1–R6 / T1–T7 · **ยังไม่ได้สัมภาษณ์เต็มรูปแบบ ไม่มีคำตอบข้อใดถูกเดาแทนเจ้าของโปรเจกต์
  ไม่มีมติใดเปลี่ยน ไม่มีโค้ดถูกแตะ** — ⛔ ห้าม engineer หยิบไปทำ ⛔ ห้าม `system-analyst`
  ออกแบบทับ จนกว่า `business-analyst` จะสัมภาษณ์จริง
- 2026-08-22 *(วันที่เจ้าของโปรเจกต์ยืนยันเองในแชท)* — **ปิด P1–P5 ของหัวข้อ pacing
  ผ่านการสัมภาษณ์ผู้ใช้จริง — พร้อมส่ง `system-analyst` ปิด A5/B3/B4** ·
  P1 บทเรียน override ได้ (`null` = สืบทอด) · P2 ยึด server env 5000/500/5000 เป็นค่าจริง
  และบันทึกว่า `lessons/new/page.tsx` (3000/800/5000) กับ `use-tutor-session.ts` (5000/1000/5000)
  เพี้ยน ต้องแก้ให้ตรงเป็นงานที่ตามมา · P3 ไม่รอ F2 · P4 backfill เป็น override ทุกบทเรียน ·
  P5 (คำถามใหม่) seed ค่าระดับบริษัทจาก server env ตอนสร้างบริษัท ทำให้เหลือ **2 ชั้นการสืบทอด**
  (บทเรียน nullable → บริษัทไม่ null) ไม่มี runtime env fallback · เตือน `system-analyst` ว่า
  pacing มีกฎการ null ต่างจาก field อื่นของ Module B · `videoDurationMs` ไม่อยู่ในรอบนี้
- 2026-08-21 *(วันที่มาจาก system context ของเซสชัน **ยังไม่ได้ให้เจ้าของโปรเจกต์ยืนยันด้วยตัวเอง** —
  เซสชันนี้ไม่มีเครื่องมือถามผู้ใช้ และเจ้าของโปรเจกต์กำลังย้ายเครื่อง)* —
  **บันทึก (ไม่ใช่ตัดสิน) ความสนใจที่จะย้ายค่า pacing ของบทเรียนไปเป็นค่าเริ่มต้นระดับบริษัท** ·
  เพิ่มหัวข้อใหม่ใน `## Open Questions` พร้อมข้อเท็จจริงที่ตรวจกับ `tutor-reducer.ts` และ
  `STATE_MACHINE.md:60` จริง, คำถามที่ยังไม่มีคำตอบ P1–P4, และการชี้ว่าเรื่องนี้ต้องปิดผ่าน
  A5/B3/B4 ของ `company-admin` · **แก้ข้อมูลที่เคยเข้าใจผิด**: A5/B3/B4 อยู่ใน
  `company-admin/design.md` ไม่ใช่ `learning-session/design.md` (ของโมดูลนี้ปิดครบแล้ว) ·
  แยก `videoDurationMs` ออกมาระบุว่าเป็นงาน UI ล้วน ไม่ต้องผ่านกระบวนการนี้ ·
  **ไม่มีการเปลี่ยน requirement ข้อใดที่มีผลบังคับอยู่**
- 2026-08-18 — สร้างเอกสารครั้งแรก แปลงจาก `docs/CORE_FEATURE_SPEC.md` (สเปกที่เคาะเมื่อ 2026-08-11)
  เป็น business requirement · ยกรายการ §6 ขึ้นเป็น Declined / Not Pursuing · บันทึก §5 ทั้ง 4 ข้อและ
  ช่องว่างเชิงธุรกิจที่พบเพิ่มอีก 4 ข้อไว้ใน Open Questions โดยยังไม่มีคำตอบ (Q1–Q8)
- 2026-08-18 — **พลิกข้อสมมติหลัก**: เจ้าของโปรเจกต์กลับมติเดิมของตัวเอง (2026-08-11) จาก
  "1 ลิงก์ = หลายการเรียน" เป็น **1 ลิงก์ = 1 การเรียน จบแล้วจบเลย** → เขียน Overview/F1–F7 ใหม่ ·
  ตัดการแยกลิงก์-การเรียน, ตารางใหม่รายคน, การย้าย 6 ฟิลด์ออกจาก `TrainingSession`, `MaxAttendees`,
  ปุ่มเรียนอีกครั้ง และหน้าที่แยกคนของ `LearnerKey` ลง Declined · ปิด Q1 (ชื่ออย่างเดียว ไม่เอา PII →
  ไม่ต้อง gate ด้วยเหตุ PII), Q5 (หมดอายุแล้วเรียนรอบที่ค้างต่อจนจบ ห้ามเริ่มใหม่), Q6 (ลิงก์เป็นกุญแจ
  ในตัว เปิดซ้ำเรียนต่อได้ ยอมรับ trade-off ว่าใครถือลิงก์ก็เห็น), Q7 (ตกไปทั้งข้อ), Q8 (ครบสไลด์ **หรือ**
  กดจบเอง → เก็บสองค่าแยกกัน) · เหลือ Q2–Q4 ให้ `system-analyst` (Q2/Q3 แทบตกไปเอง, Q4 สำคัญขึ้น) ·
  Scope หดลงมากเพราะโครงสร้างข้อมูลเดิมในโค้ดถูกต้องอยู่แล้วเป็นส่วนใหญ่
  → **⚠️ รายการนี้ถูกเพิกถอนทั้งรายการโดยรายการถัดไปในวันเดียวกัน ไม่มีผลบังคับแล้ว**
- 2026-08-18 (หลังรายการก่อนหน้าในวันเดียวกัน) — **เพิกถอนการพลิกเป็น 1:1 ทั้งหมด กลับไปใช้หลักการ
  เดิมของสเปก 2026-08-11: "1 ลิงก์ = หลายการเรียน แยกคนละคน"** · เหตุผล: เจ้าของโปรเจกต์อธิบาย
  use case จริงชัดขึ้น — **ลิงก์คือสื่อการสอนที่ส่งลงกลุ่มไลน์ ใครก็หยิบไปใช้ได้จนหมดอายุ
  ห้องเรียนเกิดตอนผู้ใช้กดเข้าและระบุชื่อตัวเอง แต่ละคนเรียนตัวใครตัวมัน 1:1** ซึ่งโครงสร้าง
  1 ลิงก์ = 1 การเรียน รองรับไม่ได้ · ยก use case นี้ขึ้นเป็นย่อหน้าเปิดของ `## Overview` ·
  เขียน F1 (แยกลิงก์ออกจากการเรียน), F1b (ตารางใหม่ "การเรียนของแต่ละคน"), F3 (`LearnerKey`
  ทำสองหน้าที่), F5 (ปุ่ม "เรียนอีกครั้ง"), F7 (ย้าย `SessionQuestion`/`ChatMessage` ไปผูกกับ
  การเรียน) และ F8 (ช่อง `MaxAttendees` กรอกได้แต่ยังไม่บังคับใช้) ใหม่ · **แก้คำตอบ Q6**
  จาก "เปิดลิงก์เดิมแล้วเรียนต่อได้เลย" (ใช้ไม่ได้เพราะลิงก์เป็นของกลาง ระบบไม่รู้ว่าเป็นของใคร)
  เป็น "ล้าง storage/เปลี่ยนเครื่อง = กลายเป็นคนใหม่ ยอมรับได้ รวมถึงที่ CS แยกไม่ออกเหมือนกรณี
  ชื่อซ้ำ" · **แก้คำตอบ Q7** จาก "ตกไปทั้งข้อ" เป็น "โชว์ช่องให้กรอก แต่ยังไม่บังคับใช้ +
  ข้อความกำกับใน UI" โดยแถว Declined 2026-08-11 (บังคับใช้จำกัดจำนวนคน) ยังคงอยู่ตามเดิม ·
  **Q2–Q4 กลับมาเปิดเต็มตัว** (มีตารางใหม่แน่นอนแล้ว ชื่อ `TrainingSession` ไม่ตรงความหมายอีกต่อไป) ·
  Scope กลับมาใหญ่เท่าเดิม — เป็นงานเปลี่ยนโครงสร้างข้อมูล + migration ไม่ใช่งานเติมฟิลด์ ·
  บล็อก Declined ของการพลิก 1:1 คงไว้แต่ทำเครื่องหมายว่าเพิกถอน เพื่อให้ประวัติอ่านย้อนได้
- 2026-08-18 — **แก้ F3: ห้าม resume เงียบๆ ต้องถามยืนยันก่อนเสมอ** · ที่มา: `system-analyst`
  วิเคราะห์แล้วพบว่ากติกาเดิม ("เปิดลิงก์เดิมแล้วเรียนต่อได้ทันที ไม่ต้องกรอกใหม่") ไม่ครอบกรณี
  **เครื่องที่ใช้ร่วมกัน** (คอมกลางในโรงเรียน/ห้องสมุด) — กุญแจอยู่ที่เบราว์เซอร์ ไม่ใช่ที่คน
  คนที่สองจึงถูกพาเข้าไปเรียนต่อของคนแรก เห็นความคืบหน้า+คำถามของคนแรก และคำถามใหม่ไปกองรวมกัน
  ในรายงาน CS · เจ้าของโปรเจกต์เคาะทางแก้: ถามยืนยัน "คุณคือ *(ชื่อที่บันทึกไว้)* ใช่ไหม"
  พร้อมทางเลือก "เริ่มเรียนใหม่ในชื่ออื่น" — เลือกวิธีนี้เพราะไม่ต้องเพิ่ม login/OTP
  (ซึ่งยังคงเป็นข้อห้ามเดิม) · ต้นทุน = ผู้เรียนกดยืนยันเพิ่มหนึ่งครั้งตอนกลับเข้ามาหลังเน็ตหลุด
  ยอมรับแล้ว · แยก F3 เป็นสองกรณีให้ชัด — **กรณี ก ไม่มีกุญแจเลย** (คำตอบ Q6 เดิม
  ล้าง storage/เปลี่ยนเครื่อง = กลายเป็นคนใหม่) **ยังคงเดิม ไม่ขัดกับมติใหม่** ·
  **กรณี ข มีกุญแจแต่พิสูจน์ไม่ได้ว่าเป็นคนเดิม** = กติกาใหม่ · อัปเดตบรรทัด F3 ใน MVP
  และ Constraints ให้ตรงกัน
