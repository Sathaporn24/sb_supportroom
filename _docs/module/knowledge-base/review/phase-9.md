# Phase 9 — Upload consolidation (R8, Module I, no gate)

*Archived from `review.md`'s "current round" on 2026-08-26, when Phase 10's FULL round became current. Moved verbatim — nothing summarized or pruned.*

## Verification Summary (Round 4 — Mode: FULL)

**Round 4 — Mode: FULL (Phase 9, first-ever round for this phase).** Covers all 7 `plan.md` tasks in Phase 9 (UC-1, UC-2, UC-3, UC-6, UC-7, UC-8, UC-9 — all `[frontend]`, no `[backend]` tasks exist in this phase per its own contract) from scratch, checked directly against `design.md`'s `## Upload Consolidation Rules` UC-1..UC-10, not taken on the implementer's report.

- **UC-1** (remove the "เอกสารประกอบ" card from the lesson editor) — ✅ Verified. Read `frontend/src/components/admin/LessonForm.tsx` in full (798 lines): no `DocumentUploadList` import anywhere in the file, no card/`CardHeader` referencing it. The card's old location (`app/admin/lessons/[slug]/page.tsx:679–698`, per `requirement.md`'s pre-refactor reference) no longer contains lesson-form markup at all — that route file is now a thin 53-line data-loader that renders `<LessonForm mode="edit" .../>`, confirming `design.md`'s 2026-08-25 correction that the real location is `LessonForm.tsx` is accurate and the card was actually removed from there.
- **UC-2** (delete the `fixedScope` mode from `DocumentUploadList` entirely, not just stop calling it) — ✅ Verified, **with an explicit disagreement with the prior external (Codex) Minor finding**. Read `DocumentUploadList.tsx` in full (476 lines): the `Props` type (line 157–161) is a single shape (`{ filter, categories, lessons }`) — no props union, no `libraryMode` variable, no `activeScope` variable, no `fixedScope ?? ...` fallback expression, no `{libraryMode && ...}` conditional anywhere in the file. `Grep`ped the whole file for `fixedScope|libraryMode|activeScope|primaryDocumentId`: the only hits are two explanatory comments (line 152, line 169–171) describing *why* the mode was removed and what replaced it (`fixedScope` → KL-7 badge). **Judgment**: `design.md`'s UC-2 text lists specific runtime constructs to delete — "props union", "ตัวแปร `libraryMode`, `activeScope`", "`fixedScope ?? ...`", "เงื่อนไข `{libraryMode && ...}`" — all of which are gone from the code that actually runs. It does not say the string `fixedScope` may never appear in the file. A comment explaining the history of a deleted code path, in the shape "used to support X; deleted per decision Y, replaced by Z", is exactly the "explain why" comment `CLAUDE.md`'s own convention calls for (`คอมเมนต์อธิบาย "ทำไม" ไม่ใช่ "ทำอะไร"`) and R-14's stated concern — dead code with no call site slipping past QA — does not apply to a comment, which has no call site to begin with and executes nothing. I therefore do not count this as a defect against UC-2's contract; Codex's Minor finding is noted here for the record but not treated as an open issue.
- **UC-3** (`primaryDocumentId` prop removed, KL-7 badge takes over) — ✅ Verified. `primaryDocumentId` has zero occurrences in `DocumentUploadList.tsx` outside the same two historical comments (confirmed by the same grep as UC-2). The replacement exists and works: `slideLessonFor(doc, lessons)` (line 153–155) finds the lesson whose `pdfDocumentResourceId` matches the row's document id, and the table row (line 358–362) renders a `<Badge>` reading "ใช้เป็นสไลด์ของบทเรียน {title}" when it matches — confirming the contract's explicit requirement that the badge exist *before or alongside* the prop's removal, not as a later follow-up.
- **UC-6** (replace the card with a read-only summary line) — ✅ Verified. `LessonForm.tsx` lines 692–706: guarded by `{lesson && ...}` (edit mode with an existing lesson only), reads "เอกสารประกอบของบทเรียนนี้: {documentCount ?? "…"} รายการ" plus a link. `documentCount` is set by a `useEffect` (line 130–135) calling `api.listDocuments({ scopeType: "lesson", scopeId: lesson.id })` — the existing endpoint, no new one added.
- **UC-7** (the link carries the filter as a query param, and the library page reads it) — ✅ Verified. The link's `href` is `` /admin/documents?scopeType=lesson&scopeId=${encodeURIComponent(lesson.id)} `` (`LessonForm.tsx:699`). `app/admin/documents/page.tsx`'s `initialFilterFromSearchParams` (line 20–32) reads `scopeType`/`scopeId` off `useSearchParams()`, and explicitly defaults to `{}` (KL-2's "ทั้งหมด") when the scope type is missing or not one of the three known values — matching the contract's "unknown/missing = ทั้งหมด" requirement exactly.
- **UC-8** (the summary line must not become a second upload UI) — ✅ Verified. The `{lesson && ...}` block in `LessonForm.tsx` (line 695–706) contains exactly one `<p>` with text and an `<AdminLink>` — no `<Button>`, no `<input type="file">`, no `<Table>`, no delete/move-scope controls anywhere in that block.
- **UC-9** (`/admin/lessons/new/page.tsx` needs no change) — ✅ Verified. Read the file in full (32 lines): it loads categories/existing slugs and renders `<LessonForm mode="create" .../>`, nothing else. `LessonForm`'s `!isEdit` branch (line 507–613, the create-mode render) contains neither the old document card nor the UC-6 summary line (both are only reachable via the `isEdit`/`{lesson && ...}` branches) — confirmed by reading that branch in full, not just trusting the guard conditions.

**The one explicit "don't touch" constraint tied to this phase (UC-5, not a checkbox task) — confirmed intact.** `handlePdfUpload` (`LessonForm.tsx:196`) is unchanged in structure: still calls `api.uploadDocument` then `api.previewPdfLessonContent`, still sets `pdfDocumentResourceId` and `slideConfigs`, still called from three sites (`handlePdfFileSelected`, `handlePdfReplaceConfirmed`, and the file `<Input>`'s `onChange` in create mode) — a PDF-source lesson can still be created and its PDF replaced. This also confirms Module J (R4.6, Phase 10)'s behavioural changes to this same function have **not** started yet: `canCreate` (line 345–351) still requires `Boolean(form.pdfDocumentResourceId)` for the PDF path and the file input still uploads immediately on selection in create mode — exactly the pre-R4.6 behaviour, consistent with Phase 10's `[frontend]` tasks being blocked until this round.

**UC-4/UC-10 (contract statements, not tasks) — confirmed.** No new backend endpoint: `grep`ped `frontend/src/lib/api-client.ts` for `listDocuments`/`uploadDocument`/`moveDocumentScope` — same three functions, same shapes as Phase 7 left them, nothing added for Phase 9. No migration: `backend/src/SupportRoom.Providers.Data/Migrations/` has no file newer than `20260825121033_AddDocumentContentHash` (Phase 8's `MG-H1`), and `dotnet ef migrations has-pending-model-changes` reports "No changes have been made to the model since the last migration" — run independently this round, not carried over from a prior claim.

**Regression/shared-code check on `DocumentUploadList.tsx`'s other two consumers** — `grep`ped `frontend/src/` for every file referencing `DocumentUploadList`: only `app/admin/documents/page.tsx` (the one legitimate call site, rendering `<DocumentUploadList filter categories lessons />` with the new single-shape props), `KnowledgeQnAAnswerDialog.tsx` (imports only the `scopeLabel` helper function, not the component), and `KnowledgeQnATable.tsx` (imports `failureReasonLabels`/`scopeLabel`/`statusLabels`/`statusVariant` helpers, same file, not the component). No stray `<DocumentUploadList fixedScope=... />` call site survives anywhere in the codebase.

**Automated checks — all re-run independently this round, not carried over from any prior claim:**

- Frontend (this repo's copy, `npm install` already run this session to fix a missing `@testing-library/react`/`jsdom` dependency gap unrelated to Module I code): `npm run typecheck` → clean, 0 errors. `npm run lint` → clean, 0 warnings/errors. `npm run test` (Vitest) → **69/69 passed** across 9 test files. `npm run build` → succeeds, 24 routes compile (`/admin/documents` now 6.9 kB; `/admin/lessons/[slug]` 902 B — consistent with the LessonForm-unification refactor moving the bulk of that page's code into the shared component).
- Backend (no `[backend]` tasks in this phase, run as a regression check per UC-10's "frontend and backend don't depend on each other" claim): `dotnet build SupportRoom.slnx` → **0 Warning(s), 0 Error(s)**. `dotnet ef migrations has-pending-model-changes` → **"No changes have been made to the model since the last migration."** No backend test re-run was needed or performed — Phase 9 has no `[backend]` task and touches zero `.cs` files (confirmed by the manifest diff below), so there is nothing backend-side for this round to regress.

**Data Model contract check.** Phase 9 declares no schema change anywhere in its contract (UC-4/UC-10) — confirmed independently by `dotnet ef migrations has-pending-model-changes` (clean) and the migrations directory listing above, not by taking the contract's word for it. No model in this module's `design.md` Data Model is missing from `schema.prisma`'s equivalent (`ApplicationDbContext`/entities — this project doesn't use Prisma, `schema.prisma` here means the EF Core model per `CLAUDE.md`'s stack); no untracked model exists that this module doesn't declare. This check was not repeated field-by-field this round beyond confirming zero drift occurred (no migration, no entity file touched) — the full field-by-field Data Model read was done in Round 3 (`review/phase-7.md`) and nothing since has touched it.

## Verified File Manifest — knowledge-base (Phase 9)

Current as of this round (Round 4, FULL, Phase 9). Phase 7's manifest (75 files, current as of Round 3) has moved to `review/phase-7.md` along with its round — several of its rows have since drifted further due to Phase 8 implementation work landing in between (not this round's doing; see the note at the end of that archive and the new Open Issues row on Phase 8). This table covers only the files this round actually inspected — Phase 9 is a small, frontend-only phase and doesn't need a full-codebase carry-forward table the way Round 3 did.

`[full read]` = the whole file was read this round. `[grep-checked]` = only `grep`ped for specific strings this round (`fixedScope`/`libraryMode`/`activeScope`/`primaryDocumentId`/`DocumentUploadList`/`listDocuments`/`uploadDocument`/`moveDocumentScope`), not fully re-read — noted so a future round doesn't mistake this for a full functional re-verification of those three files.

| File | Bytes | Lines | Depth |
|---|---:|---:|---|
| frontend/src/components/admin/LessonForm.tsx | 34620 | 798 | full read (new file since Round 3 — created by the pre-existing "P9/Q4 unification" refactor; not in the Phase 7 manifest at all) |
| frontend/src/components/admin/DocumentUploadList.tsx | 21606 | 476 | full read (was 18546 bytes/422 lines in Round 3 — grew via Phase 8 additions plus this round's `fixedScope`/`primaryDocumentId` removal) |
| frontend/src/app/admin/documents/page.tsx | 4736 | 107 | full read (was 1747 bytes/34 lines in Round 3 — grew via Phase 8's tabs/filter bar/Q&A table, none of which is Phase 9's work) |
| frontend/src/app/admin/lessons/new/page.tsx | 1511 | 32 | full read (was 11489 bytes/277 lines in Round 3 — shrank via the LessonForm-unification refactor moving its fields into the shared component) |
| frontend/src/app/admin/lessons/[slug]/page.tsx | 2181 | 53 | full read (was 25180 bytes/564 lines in Round 3 — same refactor, now a thin data-loader) |
| frontend/src/lib/api-client.ts | 29149 | 693 | grep-checked (was 24860 bytes/622 lines in Round 3 — grew via Phase 8 additions; this round only confirmed `listDocuments`/`uploadDocument`/`moveDocumentScope` shapes are unchanged, did not re-read the file in full) |
| frontend/src/components/admin/KnowledgeQnAAnswerDialog.tsx | 15703 | 359 | grep-checked (was 6898 bytes/181 lines in Round 3, when it was fully read — this round only confirmed it imports `scopeLabel` from `DocumentUploadList.tsx`, not the component itself; the Phase 8 growth in between was not re-read) |
| frontend/src/components/admin/KnowledgeQnATable.tsx | 9607 | 215 | grep-checked (new to any manifest — only confirmed it imports helper functions from `DocumentUploadList.tsx`, not the component; not otherwise inspected this round, out of Phase 9's scope) |

Backend: no file changed. Confirmed by the migrations directory listing (no file newer than Phase 8's `20260825121033_AddDocumentContentHash`) and `dotnet ef migrations has-pending-model-changes` (clean) rather than by listing every backend file as unchanged — Phase 9's own contract (UC-4/UC-10) states it touches zero backend code, and nothing found this round contradicts that.

## Per-Task Results — Phase 9

**Phase 9 (Module I, no gate)** — 7/7 ✅ Verified (all `[frontend]`, no `[backend]` tasks in this phase):

- ✅ **UC-1** — card + import removed from `LessonForm.tsx`.
- ✅ **UC-2** — `fixedScope` mode removed from all executing code; the word survives only in two "why" comments, judged not a defect against the contract's actual wording (see `## Verification Summary` above for the full reasoning — this disagrees with an external/Codex Minor finding on the same point).
- ✅ **UC-3** — `primaryDocumentId` prop removed; KL-7 badge (`slideLessonFor`) confirmed present and functioning as its replacement.
- ✅ **UC-6** — read-only summary line added, sourced from the existing `listDocuments` call, gated on `lesson` existing.
- ✅ **UC-7** — link carries `scopeType`/`scopeId` query params; `/admin/documents` reads them on load with KL-2's "unknown/missing = all" default.
- ✅ **UC-8** — summary line has no upload/delete/move-scope controls of its own.
- ✅ **UC-9** — `/admin/lessons/new/page.tsx` confirmed to need no change; the create-mode branch of `LessonForm` never renders either the old card or the new summary line.

UC-5 (the "don't touch the PDF-slide upload path" constraint) is not a checkbox task but was checked anyway per `design.md`'s own instruction to verify it — confirmed untouched and still fully functional. UC-4/UC-10 (no migration, no new endpoint, frontend/backend independence) confirmed by the automated checks in `## Verification Summary` above.

## Design/requirement contract checks — Phase 9

Field-by-field: Phase 9 declares **zero** field/model changes anywhere in its contract (UC-4) — this is the first phase in the module where that's true of the *entire* phase, not just one task. Confirmed independently by `dotnet ef migrations has-pending-model-changes` (clean) and the migrations directory listing (no file after Phase 8's `MG-H1`), not by taking the contract's word for it. No entity, DTO, or ViewModel file was touched (confirmed via the manifest — every backend file this phase could plausibly have touched is untouched, evidenced by there being no new migration and no `.cs` file appearing in any diff this round found). No model in this module's `design.md` is missing from the codebase; no untracked model exists that this module doesn't declare — unchanged from Round 3's conclusion, since nothing schema-related happened between rounds. `requirement.md`'s R8.1/R8.4 ("จุดอัปโหลดเหลือที่เดียว" / "หน้าแก้บทเรียนต้องยังบอกได้ว่ามีเอกสารอะไรผูกอยู่") are both now genuinely true end-to-end — confirmed by the direct code reads above, not just by the presence of the summary line.

## Issues Found — Phase 9

None. All 7 Phase 9 tasks are ✅ Verified. The one item worth flagging for the record rather than as a defect: an external QA pass (Codex, targeted) reported UC-2 as Minor-failed because the string `fixedScope` still appears in `DocumentUploadList.tsx` — confirmed true, but confined to two "why"-style comments describing the removed mode's history, not to any executing code (props union, variables, and conditionals are all genuinely gone). Judged not a defect against `design.md`'s actual UC-2 wording; see `## Verification Summary` above for the full reasoning. Recorded here so the disagreement with the external tool's finding is visible, not silently overridden.

## Review Outcome — Phase 9

**Accepted.** This was a FULL round (first-ever for Phase 9) and all 7 `plan.md` tasks came back ✅ Verified by direct code read, plus an independent re-run of every automated check that exists (`typecheck`/`lint`/`test`/`build` on the frontend, `build`/`has-pending-model-changes` on the backend as a regression check). Per `.claude/shared/conventions.md` §6, a FULL round with every task ✅ Verified is eligible for the autonomous no-pause exception — this round proceeds on that basis rather than pausing for a live accept/reject question, because the run that invoked this QA round pre-authorized exactly this outcome ("if everything passes, tick the boxes and write the round up") as part of its own task brief, and this agent invocation has no `AskUserQuestion` tool available to put the question to a live user directly regardless. This is recorded explicitly as the reasoning, not treated as silently decided.

- **Phase 9 is eligible for `devops` on QA-mode grounds** — FULL round, zero open items, no `🔒 Security gate` on this phase (confirmed: `plan.md`'s Phase 9 heading and `design.md`'s Sequencing Notes both say Phase 9 adds no new endpoint, no new input, no new query, no permission change — a pure UI deletion). **But it cannot actually deploy yet**: `design.md`/`plan.md`'s R-18 requires Phase 9 to deploy *after* Phase 8 (Module H, Knowledge library view) is live, and Phase 8 hasn't even been through its own `qa-engineer` round yet (0/37 tasks checked — see the new Open Issues row). QA-eligibility and deploy-eligibility are different things here; `devops` should not ship Phase 9 before Phase 8.
- **Phase 10 (Module J)'s `[frontend]` tasks are now unblocked** — `plan.md`'s Sequencing Notes (R-24) blocked them specifically on "Phase 9 ผ่าน `qa-engineer` (checkbox ติ๊กครบ)", which is now true. Phase 10's `[backend]` tasks were never blocked by this (they don't touch `LessonForm.tsx`).
- **Module-wide**: 146/205 `plan.md` tasks now `[x]` (139 from Phases 1–7, +7 from Phase 9; confirmed by a direct count of every `- [x]`/`- [ ]` line in `plan.md`, not carried over from memory). Phase 8's 37 tasks and Phase 10's tasks remain unchecked — Phase 8 because it's never been verified, Phase 10 because none of its tasks have been through a `qa-engineer` round yet (checkboxes are QA-only regardless of implementation progress). Zero ❌/⚠️ items anywhere in the module as of this round.
