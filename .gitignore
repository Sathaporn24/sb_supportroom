# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env
# Tracked on purpose so the team can clone and run without re-collecting keys.
# THIS FILE HOLDS LIVE CREDENTIALS - keep this repo private, and rotate every key in
# it (Supabase service_role, Google service account, Gemini) before ever making the
# repo public or removing a collaborator.
!.env.local

# typescript
*.tsbuildinfo
next-env.d.ts
